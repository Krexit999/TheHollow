# THE HOLLOW — UNBUILT LEDGER

> Split from DESIGN.md. The ledger is a claim, not evidence (see PILLARS.md working rules).

## UNBUILT — the deferral ledger

Every item cut or deferred from a phase, which phase it came from, and why — so a later
phase can check whether the reason still holds (a cut is provisional; its reason can
dissolve). **Append to this whenever anything is deferred.** Backfilled by auditing the
appendices.

**The ledger is a claim, not evidence** (see the working rule below). This table was
rebuilt at A.35 against the codebase, not from memory: every row's status was set by an
actual reference check (the file and symbol are named in the row), after two rows —
gem cutting and rune casting — were found marked UNBUILT while their implementations had
already shipped in P18. Verify a row against the code before you act on it.

| Item | Phase | Status | Verified against | Note |
|---|---|---|---|---|
| **Sound / audio** | P17 Part 4 (A.27) | UNBUILT | No `AudioContext`/`Audio`/`playSound` anywhere in `src/` (A.35 grep, zero hits) | The most-regretted cut: early boredom ties to everything after it, but sound done badly (a global audio context fighting `prefers-reduced-motion`, autoplay before opt-in) is worse than silence. Deserves a phase where it is the headline. |
| **Trait-driven visual identity** (a tool/material *looks* like its traits) | P17 Part 4 (A.27) | PARTIAL | Trait colour in renderers; no systematic per-trait visual language | Cut alongside sound; some trait-driven colour exists, but a full per-trait visual grammar was not built. |
| **Gem cutting** (shape a raw gem before socketing) | ~~P16 Part 4~~ → **built P18** | **BUILT** | `finishCut` + `workbench.gemCuts` in `workbenchActs.ts:197`; `cut` act live in `CraftWorkbench.tsx` | **Ledger correction (A.35):** was marked UNBUILT from memory; it shipped in the P18 Workbench as the `cut` craft-act. A raw gem is cut (lean + quality 0..1) before it socket-reads. |
| **Gem fusion** (combine gems) | P16 Part 4 → **built A.35** | **BUILT (A.35)** | `fuseGems`/`fuseGemsPreview` in `workbenchActs.ts:307`; live in `CraftWorkbench.tsx:500` | Two duplicates fuse into a better cut for the type — non-destructive (quality never falls), diminishing toward a 0.7 cap below a clean hand-cut, so skill still beats grinding. |
| **Tool rune-slots** (more room on a better tool) | P16 Part 4 → **built A.35** | **BUILT (A.35)** | `runeSlots(state,target)` in `content/shell4/runes.ts:117` (tool I–V:3, VI–X:4, XI–XV:5; gear fixed 3) | Tier buys sequence length; a longer sequence is where triples live. |
| **Rune extraction** (pull a seated rune back out) | P16 Part 4 (A.26) | PARTIAL | `salvageTool(..., extract)` recovers runes if paid | Extraction exists via salvage; a dedicated non-destructive un-seat was not built. |
| **Heirloom narrative history** (a tool remembers its deeds) | P16 Part 4 → **built A.35** | **BUILT (A.35)** | `heirloom.ts` (`HEIRLOOM_MARKS`, `addToolMark`, `logToolDeed`); hooks in combat/drops/breach/recursion; badges in `ForgePanel.tsx` | Marks (warden / recursion / geodes×100 / breach) read on the tool, each a small capped touch of `dropRate`. Distinct from P17 heirloom *composition* (parts through a Recursion), which already existed. |
| **Rune casting from materials, hand-craft vs delegate** | ~~P17 Part 3~~ → **built P18** | **BUILT** | `finishCast` + `CAST_RECIPES` + `matchCast` in `workbenchActs.ts:228`; `cast` act live | **Ledger correction (A.35):** was marked UNBUILT from memory; casting shipped in the P18 Workbench (a `cast` act reads input traits → a rune). A.35 added `runes.castKinds` so a cast rune is counted apart from a found one. |
| **Salvage: bulk / auto-salvage** | P16 Part 2 → **built A.35** | **BUILT (A.35)** | `bulkSalvage` in `salvage.ts:95`; two live buttons in `ForgePanel.tsx:59` | Break down every obsolete tool at once (below the equipped tool's tier), optionally paying to keep runes/gems. Never touches the equipped or the last tool. |
| **Tempering: the deeper media tree** | P16 Part 3 → **deepened A.35** | PARTIAL | `temperBonus` scales active bonus by `1 + 0.4·affinity` in `tempering.ts:129` | Deepened this phase: a temper now RESONATES with the tool's shell affinity (temper × where you are × the history in your hand). A broader tree of new quench media is still room to grow. |
| **The Face Cluster — Kiln + Drill Bay** | A.33 | **BUILT (A.34)** | (resolved) | Deferred at the A.33 green checkpoint; completed in the follow-up. Kept as a resolved entry — the ledger records outcomes, not just open items. |
| **Delver skill tree — the full 22/branch (66-node) size** | A.36 | PARTIAL | `SKILL_NODES` in `content/shell1/skillTree.ts` — **24 nodes** (Loam's opening set + more opening across shells II–VII via `unlockBreach`) | A.36 fixed the reported bug — the tree now OPENS PROGRESSIVELY across all seven shells and points no longer dead-end in Loam. But the locked spec is 3 branches × ~22 nodes (66); the game has 24. Reaching the full count (and spending all ~260 points earned by L200) is a balance/content pass, out of scope for a fix. The stubs are gone; the shape is real but shorter than the spec. **Re-counted A.37: 24, not 25.** |
| **Core tree — spec'd depth** | A.37 (Part 0) | PARTIAL (honest) | `CORE_NODES` in `content/shell1/coreTree.ts` — **14 nodes**; spec sketched ~28 | Unlike the old skill-tree stub, this is NOT deceptive: the 14 are real, tranche-gated nodes with no fake "opens later" UI promising nodes that never arrive. The shortfall is a shorter tree, not a lie about its length. Doubling it out is a Core-economy balance pass, out of scope for a legibility phase. Ledgered here so the gap is on the record, not hidden. |
| **Echo-layer income attribution in-game** | A.39 B3 | PARTIAL | `scripts/echo-share.ts` (the strip-replay measure); Resonant Memory card in `ferrite.tsx` now states carried strength per signature | The sim measured the echo layer at 19.6–25.4% of engaged income post-Breach-2/3 — but ~14–20pp of that is carried signatures (chipMult chains the modifier breakdown never shows), so Echoes FELT worth 3–8% while measuring ~18%: a legibility gap, not a balance one. B5-era fix shipped the carried-strength line on the Resonant Memory card. A full in-game attribution ("your carry is worth X% of income right now") needs a live strip-measure or a cached estimate — real work, ledgered, not cut silently. |

| **Signature techniques III–VII** (growth, refraction, pressure, absence verbs) | A.40 Part B | STUBBED BY RULING | `registerTechnique` registry in `src/engine/techniques.ts`; only `skim` (face.ts) + `poleshift` (polarity.ts) registered | The framework accommodates all six; shells III–VII await the separate creative pass (user ruling: "don't want them locked yet"). A shell without a technique shows nothing (pillar 5) and gates nothing. Fill via `registerTechnique` per shell. |
| **Keystones III–VII** | A.40 Part B | STUBBED BY RULING | `registerReferenceKeystones()` in `src/engine/systems/keystones.ts` — loam + ferrite only | No def = no gate (`keystoneSatisfied`), so stubs can never softlock. Part A proposed: Verdance rootweave brace (Loom), Glassmere ground focus (Bench), Cinder pressure seal (Vents); the creative pass rules. Law 6 in audit-recipes.ts checks any added def. |
| **Shell bands III–VII** (~15 upgrades/shell) | A.40 Part B | STUBBED BY RULING | `band: 'shell'` rows — only shell2/upgrades.ts (15 rows) exists | The Ferrite band is the reference: ⅓ behavioral, spine-priced, discovery-gated, fittings persist through Collapse. Loam's band is ALSO unbuilt (the ruling scoped Part B to framework + Ferrite; Loam got its technique + keystone as the N=2 generalization proof, not its band). |
| **Tier III+ hardness walls — the same rich/pure coupling** | A.40 close-out | OPEN, evidence gathered, scoped out by ruling | `sim-out/idle-diag.log`: `wall-blocked at loam 109: deepcutter — Short of Umberjade` then `— Short of Loamiron`. Tier-III recipes want UMBERJADE/WORMSTEEL (pure, minDepth 40) and loamiron 12 | The tier-II fix (a commons-only floor recipe) was ruled for THE FIRST wall only. The identical coupling sits at every later wall and gets worse: tier III's binding inputs are PURE-band (minDepth 40), so they earn during an even smaller slice of each Collapse cycle. Not fixed — the ruling was explicit about scope, and the same remedy at every tier is a ladder-wide balance decision (it would also mean authoring a commons/low-band floor recipe per tier, which changes what each tier's tools mean). Measure with `scripts/probe-idle-forge.ts` before ruling. |
| **THE DRILL BAY GATE SITS BEHIND THE WALL IT IS NEEDED TO PASS** | A.42 | **FIXED (A.42 part 2, ruled)** — `BAY_DEPTH_UNLOCK.depth` 55 → **40**, below Loam's first wall at 44. Enforced by `settle.test.ts` ("a structural unlock never gates behind the wall it is needed to cross"), which walks `requiredTier` to find the first wall and asserts the gate is strictly under it. The general rule is now a PILLARS working rule | `BAY_DEPTH_UNLOCK` in `content/shell1/upgrades.ts` (= 55) vs the tier-II hardness wall at loam **44** (`wall-blocked at loam 44 (tier 2)`, `sim-out/descent/diag-idleon.log`). Diagnostic h1–h7: `ceiling 4.7–13/s | realized 10% | next descend 27 (2–6s at ceiling)` — seven hours at the seepage floor. `built bayBuild` at **513m22s**; with `--bay 40`, at **69m30s** | Drills are what lift an idle player from ~10% of the field ceiling to near it. The unlock is gated on Loam depth record 55; the tier-II wall is at 44; so the thing that ends the starvation is behind the gate the starvation makes unpassable. Active passes 55 in minutes (the code comment's "45-minute beat" is true for exactly one of pillar 1's two players). **This is the whole R-spike** — R jumps 8.9 → 18.3 across four depths at that wall and falls monotonically to 6.3 by d130 afterwards. NOT FIXED: moving a structural unlock gate is an ordering ruling, not a tuning number. And the 12h `--bay 40` arm is **not usable evidence** — earlier drills trip the sim's collapse rule at depth 40 repeatedly (49 collapses, 1 drill, max depth 39), so the run never keeps the bay it unlocked. Sizing this needs a collapse policy that models a player who owns machines. |
| **The pillar-2 gate could not fail honestly — FOUR causes, three removed** | A.42 | **MOSTLY FIXED**; residual named, cause proven — straddle excluded and counted; a SECOND cause found | `sim.ts` pillar-2 block now skips windows where `collapse.count` changed and reports them (`N skipped: straddled a Collapse and cannot be read`). With them excluded, a 12h idle run STILL read `max 100%, 2/56 windows >100%` | **It is the measure, not a faucet — proven by removing three causes one at a time.** (1) *Collapse straddle*: a window containing a reset integrates the deep ceiling while paying out charge banked before it. Now skipped **and counted**. (2) *Non-field income*: the numerator was `totals[chip]`, so every coin the Guild paid read as the field over-producing (46–56 contracts/run). Now `stats.fieldChargeHarvested` — a new engine counter fed only by `takeCharge`, seepage and offline harvest. (3) *Yield multipliers*: the ratio was currency ÷ `dpsMax`, and both sides moved with Blade and crits. Now **charge ÷ charge**: harvested charge over `cells × regen`, where no multiplier exists. (4) *Storage*: the face holds `cells × cap`; a window that starts fuller than it ends is spending it, so the drain is now part of the denominator. **Residual: 102%, ~2 of 21 windows.** Cause found and proven, not guessed — `applyFieldSize` seats every new cell at full cap (`new Array(w*h).fill(cap)`, face.ts:344; the upgrade's own text says "New cells come in full"). That is charge INJECTED by design, which no window-differencing can attribute. To close it the engine must count injected charge where it is granted. Ledgered at that specific line. |
| **RTP — measured under the corrected policy** | A.42 | **COLLAPSE LAYER IN BAND**; the other three rungs unsampled | New RTP block in `sim.ts` (recovery ÷ time to first stand at that depth, + share of the following run spent re-treading). 12h idle, settling ON and OFF: **NO RETURNS** in both. A 4h run under the superseded collapse policy read median **3%**, worst **8%** | **Fixed as ruled: a 95%-of-peak tolerance.** The corrected policy collapses AT the run maximum, so demanding that exact integer again was the hardest bar the measure could pick — the run cycles at 144–149 of 150, which is back by any reading a player would recognise. With the tolerance the measure resolves: **Collapse layer median 20%, worst 20% (n=2, 4h active)** — inside pillar 6 ≤20–25%, at its edge. The other three rungs report : a 12h run reaches no Breach, so Breach/Recursion/Spiral RTP is still unmeasured, and the instrument now says so per layer instead of silently averaging. Breach and Recursion measure the climb back in the world you LAND in, capped at that world floor, since the peak shell no longer exists to return to. |
| **THE KILN BEAT — the early-band lever** | A.42 close-out → **A.43** | **CLOSED, but the row was wrong about the cause.** The lever was SEEPAGE, not the Kiln | `sim-out/a42-cadence.md`: d15 → d20 costs idle **8.6 → 33.2 min** against active’s 2.8; R peaks 7.4 at d30–35 in both gate arms; `kilnBuild` costs 500 chip and lands ~30 min into an idle run | **Two-thirds of this row was wrong and A.43 measured it.** The starting charge reaches depth **7**, not 15 (288 dust against a cumulative 800). And seventeen of the thirty-three minutes were a HARNESS LATCH, not an economy: `shop()` refused every descent above 100 dust until the Kiln existed, and `descendCost(17) = 108`. What survived the correction is the third of it that mattered — seepage was the only income, and the FRACTION was the ratio. Drills cannot help: the bay does not exist at d15 and `BAY_DEPTH_UNLOCK` has already been proven irrelevant here (d15–35 are bit-identical at gate 35 and gate 40). The candidates are the Kiln price, the seepage rate before the first converter, or an earlier idle income floor of some other kind — all three are opening-arc balance changes touching the game’s most-measured minutes (the 4-second first upgrade, the 8-minute first Collapse), which is why this is a pass and not a patch. |
| **The seepage floor is the shape under both early-band gates** | A.42 close-out → **RESOLVED A.43** | **FIXED: `SEEP_EFFICIENCY` 0.10 → 0.15**, the general fix, all seven shells | `SEEP_EFFICIENCY = 0.1` in `systems/face.ts`; every idle stall this phase measured bottoms out at exactly 10% of ceiling (`realized 10%` for seven straight hours pre-gate-fix) | The row was right, and A.43 acted on it. `SEEP_EFFICIENCY` **is** the pre-machine idle/active income ratio: active sustains 0.96–1.00 of the field ceiling (measured) and idle sat at exactly 0.100, so the constant was legislating 10× against pillar 1's ~5×. Raised to **0.15**, and the value is MEASURED not derived — the derivation says 0.20 (income exactly 5×) and overshoots time-to-depth to 3.0–3.9× because idle's bootstrap, Depth Pressure and later drills compound on top. R across d30–d60 went **7.9/7.8/7.0/8.1/8.0 → 4.0/4.5/4.1/3.7/3.7**. NOT faucet-adjacent after all: seepage takes a fraction of OVERFLOW, which is regen no drill harvested, so seep + drill ≤ regen identically at any value below 1.0 — proven per shell in `seepage.test.ts`, not argued. Active is untouched by construction and per shell in test: overflow only exists at a cell that is AT CAP, and a worked face never has one. |
| **R collapses to ~1.0 over the last ten depths of Loam** | A.43 confirmation | OPEN — n=1 per arm, needs a second seed before it means anything | 12h run, `sim-out/a43/`: d140 idle 149 m / active 152 m; d150 idle **316** / active **320**. The last ten depths cost idle 167 min and active 168, and they are most of both runs | Both arms park at the tier-III wall (pure-band umberjade/wormsteel) and wait the same length of time, so active spends its ~10x income advantage on 37 collapses against idle's 24 — each one paying a re-descent — and buys 1x frontier progress with it. Pillar 1 wants convergence to ~1.3x by mid-game, so the SHAPE is right and mildly overshot; what is unexplained is that the convergence is a WALL, not an economy. This is the same tier-III coupling ledgered at A.40 close-out, now visible from the ratio side. **One run per arm** — the identical 167/168 is either a real shared gate or a coincidence, and this project has twice been wrong from a single run (d44 vs d109). Second seed first, then diagnose. |
| **The ~100s income blackout after the opening bootstrap** | A.43 | OPEN — named, NOT fixed, by ruling | `tickFace` pays seepage only from `overflow`, i.e. cells AT CAP. The opening CSV shows minute 3 at `realized 0.000` with `cells_at_cap 0.00` | Chipping the face down to fund the first descents leaves every cell below cap, and seepage pays exactly nothing until they refill (~100 s at base regen). An idle player who taps at the start is briefly on zero income. Ruled to stay: it is honest physics (full cells leak), it self-heals, and the two minutes of smashing visible rock that cause it are the opening working — they fund the 4-second first upgrade. Recorded so it is a known shape and not rediscovered as a bug. |
| **The descend policy's spend ORDER is unmodelled** | A.43 | OPEN — a harness question with a real effect on every pacing number | `SIM_DESCEND_POLICY=latch\|reserve\|plain`. At 1.5 h idle the three reach d44 / **d99** / d44 | The `reserve` mode holds the next unlock's price back, which — through `shop()`'s bank-slice heuristic (`buyIfUnder(id, bank, 0.35)`) — incidentally front-loads Blade and Soil, and multipliers compound. So two defensible policies differ by 2.25× in depth at 1.5 h. R is robust to it (~7 under all three before A.43), which is why the seepage finding survived, but ABSOLUTE pacing is not. Any beat quoted from a sim carries the policy it was measured under; `reserve` is the current default because the `latch` it replaced was a defect. A play policy that weighs payback instead of bank-share would settle it. |
| **Pillar-2 residual: full-cap new cells are unattributed charge** | A.42 | OPEN — **bookkeeping only, low priority, accepted as not-a-faucet** | `applyFieldSize` → `new Array(w * h).fill(cap)` in `systems/face.ts:344`; the `expand` upgrade’s own text says "New cells come in full". Residual 102% in ~2 of 21 windows after three larger causes were removed | Charge injected by design at a point no window-differencing can see. The measure is otherwise clean (charge ÷ charge, field-only numerator, storage drain in the denominator, Collapse straddles skipped and counted). Fix is one counter incremented where the cells are seated, then subtracted from the window’s supply. Nothing depends on it: the gate reads 102% instead of 100%, and the cause is written down. |
| **Collapse cadence, if the Kiln beat later changes the shop-limited shape** | A.42 close-out | OPEN — parked, with a trigger | Corrected model `run ≈ 12·T·(I_final/I_mean)/f`; measured f = 29% idle / 56% active, `I_final/I_mean` 1.53 / 1.36 | The cadence target was corrected to ~25–35 min rather than the game being tuned to the void 4–12. But `f` — the share of a run’s earnings that reaches the stair — is exactly what an early-arc pass would move: cheapen the opening, or give the idle floor more, and the shop stops eating 71% of an idle run. **If the Kiln-beat pass lands, re-measure the `cadence:` line before assuming this target still holds.** That is the trigger; without it, do not re-open. |
| **The Collapse loop runs fewer, longer cycles than designed** | A.42 follow-up | OPEN — **cause named and measured, deliberately NOT tuned** | `cadence:` line in every run summary (6h): idle 7 runs median 27.7m, active 11 runs median 29.2m; `I_final/I_mean` 1.53× / 1.36×; stair share `f` 29% / 56% | The reset ladder wants 30–60 Collapses per shell at 4–12 min. Measured: the Loam arc is 1.9–3.6 h with **7–11 collapses at ~29 min** — cadence ~2.4× over its band, count ~4–6× under the ladder, which is one finding from both ends. The 12T derivation was wrong, not the number: it assumed **income is constant within a run** (a Collapse wipes the face upgrades, so a run starts poor and ends rich — 1.36–1.53×) and that **all income buys depth** (only 29–56% reaches the stair; the rest goes to Blade/Soil/Roots). Corrected: `run ≈ 12·T·(I_final/I_mean)/f`, which predicts **29.2m against a measured 29.2m** for active. Idle over-predicts (64 vs 27.7) because its runs end at the shell floor or a wall before the horizon binds, and railed re-descent halves its cumulative factor. NOT TUNED: nudging T would hide it (12 min needs T≈25s, a run that stops while the next step still costs half a minute), and this phase has already rewritten the collapse rule twice. Wants its own pass, with the instrument in hand. |
| **The active pacing family in this ledger was a harness artifact too** | A.42 | OPEN — needs a ruling on which number is the real one | Under the corrected collapse rule the active player reaches Loam d150 at **115m**; the ledgered family is ~442–495m. Neither the bay gate nor the settling touches an active player, so the game did not change — the old collapse rule did | The old rule collapsed at the first profitable depth (40) rather than when the run stalled, so every active run reset four steps short of its own wall, repeatedly. Correcting it moved the headline active beat by 4×. Also: `beats: loam floor` is now unreliable — it samples live `s.depth` and the policy collapses on arrival at the floor, so the beat rarely catches it. Use the depth log. Every pacing number in BUILD_LOG measured before A.42 inherits this. |
| **The idle/active ratio still runs ~7× between d15 and d45 — and it is NOT the bay gate** | A.42 | OPEN. Gate moved 40→35 as ruled; the peak DID NOT MOVE. Cause re-located | `sim-out/a42-cadence.md` (3 seeds, 4h, gate 35 vs gate 40): R peaks at **7.4 around d30–35 in BOTH arms**, and d15–35 are **bit-identical** between them | **Hypothesis falsified.** The gate was moved to 35 on the prediction that the R peak sits immediately before whatever depth the bay waits at, and would move down with it. It did not move — the gate cannot affect anything before the bay is reachable at all. The table names the real step: **d15 → d20 costs idle 8.6 → 33.2 minutes** (five depths, twenty-five minutes) against active’s 2.8. That is the seepage-only stretch before the KILN is raised (~30 min) — the face starts full, the player spends that charge down to ~d15, drops to the 10% floor and waits. Same class as the bay gate, one gate earlier, and no drill-bay placement can touch it because drills do not exist at d15. Gate 35 is KEPT (it improves everything from d40 down: 5.9 vs 6.8 at d45, 3.5 vs 4.1 at d100), but the early band wants the Kiln beat looked at, not the bay. |
| **The idle-to-active ratio at tier walls is DESCENT-bound, not material-bound** | ~~A.41~~ → **CORRECTED A.42** | **WRONG — superseded by the row above.** Kept for the reason trail | A.42 measured the full curve (`scripts/descent-ratio.ts`, 12h × 3 seeds × 4 arms): R rises to ~8.9 by d40, spikes to **18.3** across the tier-II wall, then falls monotonically to **6.3** at d130. It does not widen. And the descend step itself costs **2–6 seconds of income** through the whole stalled stretch | A.41 read two hand-picked depths (d35, d44) on the approach to a wall and inferred an exponentially diverging curve. The instrument that would have caught it — first-arrival time per depth, median across seeds — did not exist until A.42 built it (`--depthlog`). This is the third A.40–A.42 finding to come from an unverified harness rather than the game, and the reason PILLARS now carries "a sim result is a claim until the harness is verified". The original row follows. |
| ~~**The idle-to-active ratio at tier walls is DESCENT-bound, not material-bound**~~ | A.41 | SUPERSEDED (see above) | `sim-out/idle-fixed2.csv` + `.log`: idle first reached depth 44 at **524m** and forged tier II at **592m** — the material phase is 68m, **11%** of the crossing. `active-cross.log`: active forged tier II at **14m20s**, reached d35 at 8m / d44 at 28m vs idle's 66m / 524m (**8.3× at d35, 18.7× at d44**). Drop rates: active 12.0/min vs idle 1.09/min (11.1×) | **This corrects the A.40 row below, which named the rich-band coupling as the cause — it was ~11% of it.** Recipe cost cannot move the RATIO at all: crossing time ≈ requirement ÷ drop-rate, so cheapening a recipe speeds idle and active proportionally (measured: the commons floor moved idle's cross 633m → 603m → 592m, i.e. barely, while removing the rich dependency entirely). The ratio is set by DESCENT: `dustCost(d) = 25·1.09^d` is exponential while idle income is not, so the gap widens with depth. Note against pillar 1, which sets active ≈5× idle early: the drop economy runs at 11× and time-to-depth at 8–19×, so every material/depth gate is 2–4× harsher on an idle player than the design ratio intends. Levers, both pillar-level: idle drop supply (seepage's per-tick weight — faucet-adjacent, and B2 deliberately closed two faucets) or the descend curve's idle affordability. NOT touched without a ruling. |
| **Commons are only abundant until something eats them** | A.41 → **NAMED A.42** | **CAUSE FOUND — GUILD CONTRACTS.** Design question (should a contract be allowed to eat the ladder) still open | `scripts/sim.ts --commons` installs a ledger over `dispatch`/`tick` that attributes every consumed common to the action that took it. 3h idle, 59 total drops: the ONLY sink all run was `completeContract — bonechalk 21`. Nothing else touched a common | A.41 listed three candidates and chased none. The attribution instrument settles it on the first run: **one Guild contract takes 21 bonechalk** — three times the tier-II floor recipe's whole requirement — out of a Hold holding ~15, at a drop rate of ~20/hour. Gear, warrens and lattice projects consumed **zero**. Two things are true and only one is a bug: (a) the contract is a legitimate, priced sink, and (b) nothing tells the player it competes with the ladder's floor recipe for the same stone, and the sim's policy takes any contract it can complete. Fixing it is a design call — decline-cost signalling on the contract board, a commons floor the board will not ask past, or contracts that ask for banded/rich stone instead — not a number, so it is not taken here. The instrument stays: `--commons` re-runs the attribution in one line. |
| **Idle variance at the first hardness wall** | A.40 close-out | **FIXED** (A.40 close-out) — kept for the reason trail; its stated CAUSE is corrected by the A.41 row above | `sim-out/idle-diag.log` (12h idle, non-quiet): `built forgeBuild 37m48s`, four `wall-blocked at loam 44: loamironPick — Short of Loamiron` (520–610m), then `forged loamironPick (tier 2)` at **633m** → ended depth **109**, 10 collapses, 7 drills, 448 drops. Versus `prog-idle.log` (same code/policy/hours): depth **44**, 0 tools, 346 drops. Probe `scripts/probe-idle-forge.ts`: 346 real drop rolls → loamiron 16 / marl 63 / ochre 70 vs the recipe's 8/10/4 → craft SUCCEEDS; requirement floor ≈200 drops | **Harness RULED OUT and game path PROVEN**: the policy does attempt the forge (the refusals name the exact short input) and does cross. The two 12h runs differ only by RNG. The binding input is LOAMIRON (rich, minDepth 10) and every Collapse resets depth to 0, so the rich band is only earning part of each cycle — that coupling, not drop volume, is what makes the first wall slow and variable (≈10.5h in the lucky run, >12h in the unlucky one) against roughly an hour active. Nothing to fix without a design ruling; if a pass is opened, the lever is the rich-band depth coupling, not the recipe cost. |
| **THE PRESTIGE LADDER WAS STARVED BY CONSTRUCTION** | A.44 | **FIXED (ruling 2)** — echo divisor 500→200, axiom divisor 25→8, `gridSlotCost` 1+floor(1.5n)→1+floor(n/12) (full board 192→20), `licenceCost` 2+3n→1+n. Exponents untouched (the shape is locked). Pinned by outcome-shaped tests in `prestige.test.ts` + `p12.test.ts` | `echoesForCores`/`axiomsForEchoes` in `prestigeMath.ts`; `gridSlotCost`/`licenceCost` in `systems/spiral.ts`. Measured Breach 1 = 508 cores → 3 echoes (`sim-out/a43/confirm-active.log`). Sized in `scripts/a44-ladder.ts` | The divisors were sized for the **voided** 30–60 collapses/shell. At the real 7–11: 3 echoes/breach × 7 = 21 echoes, `floor((21/25)^0.8)` = **0 Axioms for a complete first Recursion**; at natural play (~130 cores at floor-arrival) the first Axiom arrived at **Recursion FOUR**. Twenty Axioms authored, four reachable ever. A full 16-slot Grid cost **192 Spiral against a lifetime supply of ~12**. The Axioms are where the fold-down lives (`firstWord`, `gentleFall`), so the layer that makes a re-climb fast was the layer nobody could reach. Sized against a RANGE of cores-per-breach, not one rate. |
| **TWO AXIOM FORMULAS, AND THE TESTED ONE WAS DEAD** | A.44 | **FIXED** — `recursionSys.axiomsFromEchoes` now delegates to `prestigeMath.axiomsForEchoes`; one formula, one place | `recursionSys.ts:37` held a hardcoded `(E/25)^0.8`; `prestigeMath.axiomsForEchoes` held an identical twin referenced by **nothing but `prestige.test.ts`**. `doRecursion` (`recursionSys.ts:67`) and `hollow.tsx:211` both granted from the recursionSys copy | Re-rating the prestigeMath copy alone changed **no behaviour whatsoever** — caught only because a live-path test (`p10.test.ts:183`) failed while the "locked formulas" suite stayed green. The project's own working rule ("a test that a function works is not a test that anything calls it") in its nastiest form: not a dead system, a LIVE system shadowed by a dead twin, with the tests guarding the twin. **Audit the other three prestige formulas for twins** — `coresForDepth`, `echoesForCores` and `spiralFor` were not checked for duplicates this pass. |
| **The recursion scenario was handed the currency it existed to test** | A.44 | **FIXED** — now computed as `echoesForCores(508) × breachCount` | `scripts/sim.ts` `--scenario recursion` set `s.totals['echo'] = D(120)` with the comment "floor((120/25)^0.8) = 3 Axioms this recursion", against a real 7-breach yield of **21** | The only harness that exercises a Recursion was seeded with 5.7× what the game pays, so the starvation could not be seen from it. Same family as the six "a sim result is a claim" precedents, one step earlier: the instrument asserted the conclusion it was built to check. |
| **The sim cannot drive a Recursion, so three RTP rungs stay unmeasurable** | A.44 | OPEN — harness gap, named not fixed | `--scenario recursion` primes aleph at depth 38 with `CORE_DEPTH = 40`, and a 6h run ends `core touched false | RECURSIONS 0`. The Axiom grant IS verified end-to-end by `p10.test.ts:156` (`dispatch({type:'recurse'})` → 5 Axioms in the purse), deterministically | Confirms and sharpens the A.42 row "the other three rungs unsampled": it is not that runs are too short, it is that **no sim policy touches the Core**. Breach/Recursion/Spiral RTP therefore has no sim path at all, and pillar 6 is enforced for one of four layers. Fixing it means an aleph policy that pushes for the Core — small, and it unblocks every future ladder measurement. |
| **The Core tree never completes at the measured cadence** | A.44 | OPEN — measured, deliberately NOT tuned this pass | `core nodes maxed 0/14` after 12h in both arms (`sim-out/a43/confirm-*.log`); ~508 cores earned across a Loam arc against SPEC's "~800 in Shell I ... tree completes around Shell V"; 14 nodes at base 2/r=1.55/10 levels ≈ 3,000 cores to max | `coresForDepth` was named as a re-rate lever and deliberately left alone: the ruling's four targets are all met without it, and raising the core faucet while lowering the echo divisor would double-count the same fix. But the Core TREE is a different sink from the Breach, and it is ~40% short at the real cadence. Wants its own sizing pass, with the ladder instrument (`ladder:` line) already in hand. |
| **RTP was verified for 1 of 4 layers — THREE blockers, not one** | A.44 | **FIXED** — per-layer pending claims; `original` supplied from the shell LEFT; the policy now dispatches `spiral` | `noteRtpCollapse`/`noteRtpTick` in `scripts/sim.ts`; `type: 'spiral'` appeared nowhere in the file before | (1) One global pending slot, so the ~15-min Collapse layer permanently starved the ~12-hour rungs — the "keep the oldest claim" reason was sound, the single slot was not. (2) `original` was read as `firstAt[landedShell][peak]`, and after a cross-shell reset you have **by definition never stood at that depth in that shell**, so it was 0 and the claim was discarded — Breach could not have recorded a sample even with the slot free. (3) The Spiral was never dispatched at all, so the Automation Grid, licences and Spiral RTP were unreachable at any runtime. |
| **The RTP instrument was fed the answer it measures (the NINTH case)** | A.44 | **FIXED** — pre-run depths marked unmeasurable; `--scenario` runs now REFUSE to print pillar-6 ratios | `--scenario recursion` primes `depthRecords` to 560 before the clock starts; `noteRtpDepth` backfilled every first-arrival to ~1s; readings came out at **433% and 813%** | Those look like catastrophic pillar violations and are division by a stipulated number. Two fixes were needed: marking seeded depths unmeasurable still left the scenario comparing "time to walk 2 depths in aleph" against "re-climb Loam from zero" (367%). **Mechanisms can be exercised from a scenario; ratios cannot** — the scenario is still the right tool for proving the Recursion PATH works, and the wrong tool for every number downstream of it. |
| **BREACH RTP MEASURED — 16%, IN BAND** | A.44 | **RESOLVED for the Breach layer** (n=1). Recursion/Spiral still unsampled | `sim-out/a44-breach/active.log`, 16h active: `breach 16% (n=1)`, `collapse 3% (n=44)`; idle arm `collapse 7% (n=30)`, never breached. Beat line: `loam floor 285.3m | BREACH 718.9m | ferrite d150 765.9m | PILLAR 6 return-to-peak: 16.5%` | **The first Breach RTP figure in this project's history**, and it took fixing three separate blockers plus disqualifying the scenario to get it. Pillar 6 now holds across TWO measured layers — Collapse 3–7%, Breach 16% — so the A.44 re-rates (echo/axiom divisors, Grid pricing, `CORE_NODE_RATIO` 1.40) are verified against the boundary the per-shell Core tree touches most. The arm breached TWICE (Ferrite 718.9m, Verdance 815.4m); the second was still owing its return at 16h, so only the first resolved. **n=1 — one run has been an anecdote in this project twice.** A second seed before this is quoted as settled. |
| ~~**Breach / Recursion / Spiral RTP — still unmeasured**~~ | A.44 | **SUPERSEDED for Breach** (above); Recursion/Spiral remain open | 48h both arms launched to `sim-out/a44-rtp/`; at 4h05m wall it had not finished and no `DONE.txt` exists. Collapse is measured: **5% median / 10% worst (n=6)** at 6h, 22% (n=1) at 4h — in band | The instrument is fixed and a real run reaching Breach (~12h) will now record; nothing has yet. Deliberately NOT filled in from `--scenario`, which the row above disqualifies from ratios. Note the wall-clock estimate that was wrong: 48 sim-hours was projected at ~67 min from a 12h Loam-only run, and past Breach 1 the run enters Ferrite+ with far more systems ticking per second — budget **4–8× that**, and prefer 16–20h runs (Breach 1 lands at ~11.8h) over 48h if only Breach is wanted. |
| **THE CORE TREE IS A PER-SHELL BUILD, AND PILLARS DESCRIBED A DIFFERENT TREE** | A.44 | **FIXED (ruling 5)** — `CORE_NODE_RATIO` 1.55 → **1.40**; PILLARS passage rewritten | `doBreach` sets `state.collapse.nodes = {}` (`breach.ts:106`). `CORE_NODES` = **14** nodes costing **7,562** at r=1.55, not "28 nodes ~6,000". Loam arc pays **478** cores (`sim-out/a44-confirm`), not ~800. Tranche 2 gated on `breachCount>=1` | PILLARS said "tree completes around Shell V" — impossible, because the reset-ladder table in the SAME document says Breach resets the Core tree, and the code follows the table. So the tree is rebought every world, and a Loam arc bought **26% of the only tranche it can reach** — not enough to max one node. Within a shell the Core tree IS the permanent income growth, which is why it was the deep-end residual after the horizon fix took ~29%. At 1.40 a Loam arc affords ~56% of tranche 1, full tree only in the last shell. **Verified: `core nodes maxed` 0/14 → 1/14 in a 6h run.** Because `coresForDepth` scales with depth while this price is flat, any future change must be checked at BOTH ends (`scripts/a44-coretree.ts`). |
| **Mycelium — payoff-floor reading** | A.39 B6 | UNRESOLVED | `scripts/b6-floor.ts` readings −10.5% / +4.8% at 900s; instrument noise ±10% | The one counted system with neither a consumed output nor a resolved ≥5% home-era share. NOT folded — the strip-replay at 900s cannot resolve a 5% floor (negative shares prove the noise band). To resolve: longer windows (3600s+) and 3+ seeds per strip, or a deterministic-RNG replay. If it still reads <5%, the ruled fold is: mycelium becomes a Greenhouse mode (the spread is already planted from Greenhouse strains). |


| **The Museum's DONATE / IDENTIFY / MOVE verbs — CUT** | A.49 | **CUT, migrated** — nothing was taken | Gone from `systems/museum.ts`; save v29 returns every donated relic to `relics.held` and drops `donated`/`pieces` | The gallery is a place you LOOK at now, so all three verbs went with the grid of empty slots they existed to fill. Identify in particular was a Scrip toll for reading a record the relic has carried on its face since A.46. If a later phase wants a research verb back, the reason this one died is that it took the thing you had just earned OFF you in order to display it — any replacement has to not do that. `identifyCost`, `STUDY_BONUS` and `MuseumPiece` are gone; `caseBonusNow` folded back into the flat `bonus`. |
| **Relic POWERS are still nine authored effects, not a generator** | A.48 → A.49 | PARTIAL — deliberate | `POWERS` in `systems/relicPowers.ts` (11 defs, 4 kinds) | The kinds (rule/trade/scaling/pair) are the design; the list is small enough that a long-lived save will have seen all of them. It wants either more authored entries or a composition rule (a power that reads a second field, as tools-from-parts did for the Forge). Not urgent while the collection layer is new. |
| **Relic awakening gives an EFFECT and a story, not an UNLOCK** | A.48 | PARTIAL — the third leg is unbuilt | `WAKING_STEPS` + `powerLive` in `relicPowers.ts`; nothing writes a route, recipe or dialogue id on waking | SYSTEM_IMPROVEMENTS asks for "dormant → small effect → bigger effect + story + **a real unlock**". Two of three shipped: waking turns a POWER on and the relic speaks a line. The unlock leg needs an owner on the other side — `ROUTES` is a fixed list and the Lamphouse has no per-relic hook — so it is a two-system change. Do it with the Merchants/Lamphouse pass. |
| **`setresin` has no producer anywhere in the engine** | A.47 live-play | OPEN — pre-existing, still out of scope | Declared in `content/exports.ts`, consumed by mirrors, granted by nothing | Genuinely unobtainable. Untouched across A.48 and A.49 because both were scoped to the collection screens and this is a Glassmere export-spine gap. |
| **THE SILICA PROBLEM — the reach audit beyond Relics/Museum** | A.47 live-play | OPEN — three systems done, the rest unaudited | A.48 proved reach for fusion; A.49 re-proved it after the re-price and added the gallery (`scripts/verify-a49.ts` REACH block: Ferrite, zero Cores, zero Scrip, zero shell currency — renders and fuses on the pile alone) | Bench / Beam mirrors / rune re-prep are still unaudited. The rule to apply is the one these two phases proved: availability is "can you use this now", never "have you ever". |

| **THE A.49 CANVAS RELIC/MUSEUM SURFACES — REVERSED** | A.49 → **undone A.50** | **CUT.** Engine kept whole, presentation thrown out | `src/ui/relics/` deleted (1,447 lines: ReliquaryView/ReliquaryCanvas/relicArt); `RelicsPanel`+`MuseumPanel` back in `longtail.tsx`; `src/engine/` untouched, 923 tests unchanged | Played as grey shapes in boxes. The procedural relic art (`drawRelic`) could not make ~unbounded relics look like distinct THINGS at 30px, so the wall and the hall both read as placeholder. **A revert was the wrong tool** — A.49 shipped engine and UI in one commit, and reverting would have restored A.48's ~320-Cores fusion wall and dropped auto-scrap, `effectiveAffixes` and the museum ownership model. So it was a forward-only removal by file. The RULING: these two screens are panels; rendered art is not retried here. What the canvas pass DID leave behind and is worth keeping: the sheet-scrim `pointerdown` lesson, the `drive.ts` 'Go on, then' dismiss fix, and the ownership-based Museum. |
| **THE DRILL BAY REWORKED — four interlocking mechanisms** | A.52 | **BUILT** | `systems/drills.ts` (feed/grain/seam/synergies), `content/drillParts.ts` (draw + head fits), `__tests__/drill-bay.test.ts` (29 assertions), `scripts/verify-bay.ts` (44 driven checks) | Was "pick the best material, clone across 24". Now: a SHARED FEED every chassis draws on (browns out to a floor, never blocks), a BIT GRAIN that specialises through use and asks a real question after a Breach, a SEAM read off the live face each second that heads are fitted against (so descending and the shell signatures turn the puzzle on their own), and six bay-wide ARRANGEMENTS found by arranging, never listed. Reach-proved in Ferrite: the feed and the re-cut are both priced in CONV, the shell's own converted currency. |
| **The bay's draw ladder is hand-sized, not sim-measured** | A.52 | OPEN — balance, not structure | `BAY_BASE_SUPPLY` 6, `BAY_SUPPLY_PER_LEVEL` 3, head draws 0.85–1.65, feed ratio 1.3 | Sized by hand so a bare bay of six fits the base feed and a fully configured 24 needs roughly level 15. The SHAPE is right and the pillar guarantees are tested; the NUMBERS have not been through the sim. Pillar 1 binds the drop economy and time-to-depth as well as income (A.42), and a brownout touches all three. Wants a run before anyone quotes it as settled — a hand-sized constant is a claim like any other. |
| **Head fit is authored per HEAD, not per shell signature** | A.52 | PARTIAL — deliberate | `DRILL_HEADS[].likes` in `content/drillParts.ts` — one ideal triple per head, no shell term | The seam already moves per shell (Verdance's vines drop out of the reading; depth is a term), so the puzzle re-opens without one. But a head that explicitly reads Ferrite's polarity map or Glassmere's beam position is the obvious next depth, and SYSTEM_IMPROVEMENTS asks for exactly it ("a bit great in Ferrite underperforms in Verdance"). The GRAIN delivers the bit half of that promise; the head half is still generic. |
| **THE A.52 DRILL CONFIGURATION LAYER — REVERSED** | A.52 → **undone A.53** | **CUT.** Heads, bits, wear, behaviours, the shared feed, the seam, the grain and the six arrangements are all gone | `content/drillParts.ts` deleted; `systems/drills.ts` rewritten to ~180 lines; `DrillState` down to `{level,timer,lastCell,name,use}`; `drill-bay.test.ts` + `verify-bay.ts` deleted; save v31 migration strips the fields and grants nothing | Everything in the row above is withdrawn, including its two follow-up rows — the draw ladder and the head-fit gap no longer have anything to be gaps in. It was mechanically sound and sim-verified in band (`sim-out/a52-bay-rtp.md`, worst 1.28x), and it was still wrong: it put a configuration screen on the IDLE layer, which is fiddling with extra steps. The lesson kept from it is the shape of A.53 — the drills stay furniture, and the interesting decision moves to a screen you go to on purpose. |
| **DRILL ALLOYS — an ability, poured at the Forge, drawn on the rock** | A.53 | **BUILT** — framework + 3 Loam abilities | `content/drillAlloys.ts` (defs, trait matching, hints), `systems/drillAlloys.ts` (runtime + the two verbs), `AlloyBench` in `ForgePanel.tsx`, arc/set/call drawing in `ui/face/FaceView.ts`, `__tests__/drill-alloys.test.ts` (28), `scripts/verify-alloys.ts` (49 driven checks incl. a Ferrite reach block) | One bay-wide slot, never per drill. THE ARC jumps a strike to charged neighbours (forked lightning on the face), THE CALL gathers under a worked cell and re-rolls its drop table deeper (inward rings), THE SET leaves worked rock soft so the next bite takes more (an ember wash and a fracture). Discovery is hint→try→confirm: traits are on every material row, the bench describes the MIX, a miss names what it leaned toward, and the ability is named and recorded the moment it exists. Alloy KNOWLEDGE survives a Breach; the physical alloy does not. |
| **THE WHOLE A.53/A.56 ABILITY SET WAS ABSTRACT, AND WAS REPLACED** | A.57 | **CUT — all fifteen, and twenty-nine authored in their place** | `content/drillAlloys.ts` rewritten (29 defs, 16 shapes, 16 figures); `systems/abilityPlans.ts` (geometry); `ui/face/abilityFx.ts` (the figures); save v35 maps the three A.53 ids onto Loam successors and drops the rest | The verdict on all fifteen was that they were INVISIBLE: "the strike takes a bigger share", "the drop rolls deeper", "the stroke reaches further" — correct, bounded, and impossible to see happening. A player watching the face could not tell an alloyed bay from a bare one, which means the system was a stat block in a costume, i.e. the exact thing the bay was rebuilt twice to stop being. The replacement rule is that **an ability is a named event with a picture**: it fires at a moment you can point at, clears a shape you can see, and draws a figure while it does. Verified in play, not asserted: `scripts/verify-a57.ts` fires all 29 through the real UI and reads `FaceView.abilityFx` back — 29/29 changed the face AND drew a figure. |
| **A.57 ability geometry: sixteen shapes, one funnel** | A.57 | **BUILT** | `buildPlan` in `systems/abilityPlans.ts`; `applyPlan` in `systems/drillAlloys.ts` | Twenty-nine abilities, sixteen shapes (block/radius/ring/line/row/split/behind/bounce/chain/charged/weak/vein/vines/mature/scatter/single) and sixteen figures. Every one resolves to a PLAN — a list of `{cell, share}` harvests plus a figure — and every plan goes through the same `strike()` an ordinary drill bite uses. That single funnel is what makes pillar 2 structural rather than argued: there is exactly one place charge leaves the field. |
| **PILLAR 2 UNDER TWENTY-NINE EXPLOSIONS — proved deterministically, measured in sim** | A.57 | **HOLDS (deterministic); sim reading recorded with its instrument bias named** | `drills-a57.test.ts` "no ability can take more charge out of the field than the field held" + "neither can ALL of them at once"; `sim-out/a57-ceiling.md` | THE DETERMINISTIC PROOF IS THE ONE THAT MATTERS: regen switched off, a known amount of charge in the rock, every ability at grade VII, then all 29 at once across a 24-drill bay — `taken ≤ held` and `taken + remaining ≤ held` in every case. An explosion cannot manufacture charge because `take = min(power, cellCharge)` and `abilityParams` clamps every `share` at 1 at the data layer. THE SIM adds the rate reading: in the CEILING-BOUND scenario every one of 31 arms sits within 0.2pp of a bare bay (102.8–103.0% load, 1.00–1.02× income) — i.e. **when the field is already saturated, no ability adds anything**, which is precisely what pillar 2 predicts. In POWER-BOUND the abilities lift realized load from 59% toward the ceiling and the spread across arms NARROWS over the run (49.0% → 44.5%): **convergence holds in both scenarios**, which is the test the brief itself proposed. The last 1–3pp the rate measure could not resolve is settled model-free by `a57-saturation.ts` (row below): no ability bay out-earns a saturated bare bay. Pillar 1 worst: **1.72×** against a ~5× bound. |
| **THE FAUCET QUESTION, SETTLED WITHOUT A DENOMINATOR** | A.57 | **RESOLVED — no faucet** | `scripts/a57-saturation.ts` → `sim-out/a57-saturation.md`. One widened high-regen field, 3h × 3 seeds: **bare×16 (saturating, no abilities) = 1.000×; repulsor×3 = 0.997×; staticoverload×3 = 0.991×; slagburst×3 = 0.985×; EVERY ability at grade VII on 3 drills = 0.995×** | After three passes the ceiling sim still could not resolve the last 1–3pp, because its own bare arm reads 102.8% of the modelled production at saturation. Arguing about that was the wrong move: the question "does an explosion create charge" has an answer that needs no model at all — **a faucet would let an ability bay out-earn a bare bay with enough machines to saturate the same rock.** It cannot beat the rock by working harder, only by making charge. No arm did. The explosive ones land at 0.985–0.997× of a sixteen-drill bare bay while running THREE drills, which is the design claim exactly: a cheaper route to the same ceiling. |
| **The A.57 ceiling sim's breach gate was wrong THREE TIMES, each differently** | A.57 | **FIXED, and recorded as method — the third one is the interesting one** | `scenario(cfg, bias)` + the `noise` floor in `scripts/a57-ceiling.ts` | (1) It subtracted each scenario's OWN bare arm: CEILING-BOUND's is 102.9% (a bay carrying nothing, so the ~3pp is the A.42 residual), but POWER-BOUND's sits at 60%, nowhere near saturation, so it showed no bias and ZERO was subtracted from arms that were saturated → 13 false breaches. (2) Taking the bias from a saturated bare bay instead fixed that and produced **32** false breaches, all at 100.0–100.2%, because the bias was sampled in a separate measurement and landed 0.1pp from the in-table one — **a knife-edge `> 1` gate measuring its own float jitter.** (3) The gate now carries a noise floor (the bare arm's own seed spread, min 0.5pp) and says so. **Final reading: 27 of 31 arms clear; four flag** — `slagburst` 102.2%, `staticoverload` 101.9%, `repulsor` 103.2%, `all` 101.4%, every one of them POWER-BOUND and wide-area. Those are precisely the four the saturation script measures at **0.985–0.997× of a saturated bare bay**, i.e. not faucets, so the residual is the instrument. The general lesson, which this project keeps re-learning: a threshold with no tolerance does not measure the thing, it measures the instrument — and a real faucet here is a tens-of-points effect, not a tenth of one. Convergence had the same defect: an absolute 2pp tolerance called 0.6%→2.8% a divergence in a scenario where all 31 arms sat within 1.4pp of each other. |
| **The saturation bias is FIELD-DEPENDENT, and the sim applies one number to two fields** | A.57 | OPEN — named, bounded, low priority | `scripts/a57-ceiling.ts` samples the bias at `expand 0 / soil 0` and applies it at `expand 6 / soil 15` | The four arms that still flag are all on the WIDENED, high-regen field, and the correction subtracted from them was measured on the stock one. A widened field has had far more charge injected by `applyFieldSize` (every new cell seated at full cap) and far more overflow for growth to bank, so its bias is larger than 2.8% — which is why four saturated arms land 1.4–3.2pp over after a correction sized for a different field. **The real fix is the one A.42 already asked for and nobody has built: a counter incremented where charge is INJECTED, subtracted from the window's supply.** With it, no bias term is needed at all and this instrument resolves to the point. Until then the model-free saturation test is the answer to any question this one cannot settle. |
| **THE READY WINDOW — a full meter waits three strokes before firing itself** | A.57 | **BUILT, found in play** | `READY_GRACE` in `systems/drillAlloys.ts`; `advanceCharges` | The first cut auto-fired the instant the meter filled, which made the brief's other half unreachable: there was no moment at which an ability was charged AND had not already gone off, so the FIRE button read 0% forever and a "saved and unleashed on purpose" ability could not exist. Found by the driver failing to click a permanently-disabled button — and fixed in the GAME rather than in the harness, per the PILLARS working rule. Pillar 1 is untouched: it still fires itself, every time, with nobody there, three strokes later than a player who chose the moment. |
| **THE BROKEN-ABILITY LIMIT** | A.57 | **BUILT** | `abilityBudget`/`loadoutUsed`/`loadoutDelta`; the budget check in `forgeDrillAlloy`; the bar in `panels.tsx`; `BUDGET_BASE` 5, `BUDGET_PER_SHELL` 4 | Every ability carries a POWER tier 1–5 and the bay may not exceed its budget. Loam gets 5 against a Loam ability set costing 9, so a Loam bay runs about half of what it knows and has to choose; Aleph gets 29, which runs all four Aleph abilities (5 each) with room to spare — the power fantasy arrived at by DESCENDING rather than by buying. The check happens BEFORE the spend, because being told you cannot fit the thing after paying for it is the wrong order. A newly DISCOVERED ability that will not fit is still discovered — you made the thing, the codex records it, it simply does not go on the rails. |
| **The drill curve escalates in bands** | A.56 → **A.57** | **BUILT** | `UpgradeDef.ratioAt` (new); `drillCount` bands ×1.35 / ×1.70 / ×2.10 / ×2.60 | A.56 moved this row to a single structural ratio (1.75), which was the right direction and the wrong shape — one exponent makes the FIRST drills as punishing as the last, and the first four are supposed to arrive easily. Four bands instead: the first four cost **40 CONV together**, 4–8 costs 209, 8–12 costs 2,792, 12–15 costs 33,537, and the sixteenth chassis alone is **21,883** against 6 for the first. `nextCost`/`totalCost`/`maxAffordable` all handle the banded form by walking the levels, which is affordable only because such a def has a small `maxLevel`. |
| **The reach rule was traded, deliberately, and the trade is asserted** | A.57 | OPEN — a weaker guarantee than A.56's, on purpose | `drills-a57.test.ts` "every ability is forgeable from its OWN shell's mineable rock" + "no shell is ability-starved — every one can pour at least four from local rock" | A.56 required every signature to be forgeable from every shell's own rock, which forced all fifteen abilities onto the five traits that exist everywhere (`charged`, `dense`, `brittle`, `trueseated`, `hollow`) — and that is a large part of why they read as interchangeable. A.57 trades it for THEMATIC signatures: Cinder wants `warm`, Verdance wants `springy`, Hollow wants `hollow`, Aleph wants `trueseated`. The replacement guarantee is weaker but real and tested: every ability is forgeable in its own shell, and every shell can pour at least four from local rock, so nothing is ever ability-starved. Materials also cross a Breach. **If a later phase wants the stronger rule back, it costs the thematic signatures — that is the trade, written down.** |
| **Two abilities shipped inert because a param graded past the size of the board** | A.57 | **FIXED — both, and the class is named** | `ring()` walks inward and `lineRun()` shrinks its skip until each finds rock | A grade-VII Heat Wave asks for radius 9 and a 6×6 face has no cell nine steps from anything, so the exact ring came back EMPTY; a grade-VII Reality Skip asks to start nine cells away, same result. Both were caught by the "every ability produces a plan" test on the first run — which exists precisely because the previous two passes shipped things that did nothing. The fix is written as "reach as far as there IS board" rather than as a clamp on the grade, because that is also the honest reading of the fiction. **The class to watch: any `grow: 'add'` on a distance parameter can outgrow a 6×6 starting face.** |
| **Every `abilityFire` event carried an empty id** | A.57 | **FIXED before shipping** | `applyPlan(..., id)`; the pop in `FaceView` reads `ev.id` | `applyPlan` read the id off the plan, which never carried one — so every firing emitted `id: ''`, the face popped no NAME, and the two-slot test could not tell two abilities apart. An ability with a working mechanism and no name on screen is the exact failure this phase exists to fix, and it very nearly shipped again one layer down in the event plumbing. |
| **The later-shell ability kinds are declared and unauthored** | A.53 → **A.56** | **CLOSED — twelve authored, two per shell for Ferrite→Aleph** (all subsequently replaced at A.57) | `DRILL_ABILITIES` = 15 rows in `content/drillAlloys.ts`; fifteen live `kind` hooks in `systems/drills.ts` + `systems/drillAlloys.ts`; `drills-a56.test.ts` asserts every def has a hook AND that each behaves differently from a bare bay | The brief asked for the framework plus three, then 2–4 per shell after. Done: Ganglock/Halfmark (Ferrite), Creepvine/Seedset (Verdance), Prismcut/Longlens (Glassmere), Slagburst/Cinderhold (Cinder), Throughline/Unmaking (Hollow), Recurrence/Everywhen (Aleph). **`resonance` was DROPPED rather than shipped hollow** — reading a shell's own signature is a real design problem and a placeholder for it is exactly the deceptive stub PILLARS warns about. The A.52 head-fit promise is therefore still unkept and is now the only thing left of this row. |
| **THE POOL GROWS AND DEEPENS — abilities unlock by shell, alloys carry a GRADE** | A.56 | **BUILT** | `reachedOrdinal`/`mixGrade`/`abilityParams`/`gradeStep`; `DrillState.fits: {id, grade}[]`; save v34 | Two axes, not one. An ability belongs to a shell and appears when you have BEEN there (`depthRecords`, floored by `breachCount`, so a Recursion does not take the pool away) — the pool GROWS. Independently the alloy's GRADE is the deepest shell any material in the pour came from, and `gradeStep = grade − the ability's own ordinal` scales its params — so the pool DEEPENS and nothing learned early becomes dead weight. Grade I is byte-identical to the shipped A.53 numbers, so no existing save changes value. Priced: a grade step costs +0.6 of the base. |
| **AIMING A POUR, because fifteen signatures shadow each other** | A.56 | **BUILT** — a deliberate exception to "never listed" | `MatchOpts.prefer` in `matchDrillAlloy`; the aim row in `AlloyBench`; the engine re-checks `state.drills.alloys` so a hand-built dispatch cannot use it as a scanner | With fifteen signatures live, a generous mix in a deep shell satisfies several and the deepest wins — which would make an old favourite progressively harder to re-pour, the exact failure this phase exists to fix. So the bench can aim at something ALREADY DISCOVERED. Not a pillar-5 hole: you cannot aim at what you have never made, a known ability already prints its signature on its card, and an aimed pour that the mix cannot carry falls through to whatever the metal really is (asserted). |
| **THE REACH TEST FOR DRILL ALLOYS WAS VACUOUS FOR THREE PHASES** | A.53 → **found A.56** | **FIXED, and it found a real gap** | `drill-alloys.test.ts` now calls `createEngine` at module scope; `allShells()` returned **[]** at that point, so the loop ran zero times and passed by doing nothing | The test that was supposed to enforce the standing reach rule — "every signature is forgeable from every shell's own mineable rock" — read `allShells()`, which is populated by `createEngine`, from a position in the file where no engine existed yet. Same family as the SELF_EXPLAINING exclusion set: a green number counting nothing. |
| **The A.53 Loam trio cannot be poured from local rock in four shell/ability pairs** | A.53 → **named A.56** | OPEN — pre-existing, pinned by test, deliberately not "fixed" | `drill-alloys.test.ts` "names the A.53 trio's reach gap exactly": `aleph/emberset`, `glassmere/emberset`, `glassmere/lodecall`, `hollow/emberset`, `verdance/lodecall` | Found the moment the vacuous test above started running. `lodecall` wants `dense: 2` and Verdance/Glassmere drop ONE dense material each; `emberset` wants `warm: 2` and Hollow and Aleph drop NO warm material at all. Survivable — materials cross a Breach, so a descending player carries the stone — and not something A.56 introduced. NOT fixed here because the two candidate fixes (re-sign two shipped signatures, or give two shells warm rock) both change discovery for a save mid-flight, for a gap that only bites a player who has spent all their old stone. The twelve A.56 signatures are all drawn from the five traits that exist in every shell (`charged`, `dense`, `brittle`, `trueseated`, `hollow`) and are asserted to reach everywhere. |
| **RICH DRILL ROUTING — paint the squares, say what it prefers** | A.55 (row: "area targeting is NOT built") → **A.56** | **BUILT** | `DrillState.zone`/`priority`; `zoneSet`/`drillPriority`/`claimRank` in `systems/drills.ts`; `setDrillZone`/`setDrillPriority` actions; `RoutePicker` in `panels.tsx`; zone remap in `applyFieldSize` | The A.55 row said area routing "wants a reason beyond completeness" and the reason arrived: with fifteen abilities that do visibly different things to the rock, WHERE a machine works became a real decision rather than a knob. It is not A.52's chore because the DEFAULTS are the old behaviour byte for byte — no zone = the whole face, no priority = follow the bay-wide hunt switch — so an idle player who never opens the menu is unaffected. Four priorities: both / oresFirst / ores / rock. The zone is remapped BY COORDINATE when the face widens, the same fix A.55 needed for pockets, because an index copy slides a painted region one cell per row. |
| **DRILL ACQUISITION — sixteen bought at a structural price, eight earned and better** | A.56 | **BUILT** | `drillCount` r 1.25→**1.75**, maxLevel 23→**15**; `systems/prizeDrills.ts` (4 sources); `newPrizeDrill`/`PRIZE_POWER`/`DrillState.slots`+`prize`; bigger chassis in `FaceView.drawDrillBody` | The bay used to cost ~5,100 CONV in total at the "Standard" spam-buy ratio, so every player arrived at the alloy decision already owning every machine it applied to. Now the LAST bought chassis alone costs ~26,500. The other eight rails are prizes from other systems — free, `PRIZE_POWER` 1.6x bite, drawn 1.55x larger, and carrying 2–3 alloy slots, which is the only place in the game two abilities compound on one stroke. Three of the four sources are achievement counts (an idle player earns them on the same rails); the fourth is 250 pockets opened. **The skill-tree leg is deliberately NOT stubbed in the UI** — `grantPrizeDrill(state, ctx, id)` is the whole hook and there is no node promising a drill it cannot deliver. |
| **The re-priced drill row is a GATE, and pillar 1 binds gates** | A.56 | OPEN — **measured as a PRICE, not as a BEAT**; the beat is the reading that matters and it does not exist | `sim-out/a56-drills.md` "The buy curve": at a MATCHED 16 chassis the new row costs **53.7x** the old (658 → 35.4k CONV). Early band barely moves — 4 chassis 1.5x, 8 chassis 4.4x — and the knee is past 12 (14.7x) | The steepness is intended and is what the brief asked for; the risk is that a bought drill is the IDLE player's primary income engine, and A.42 established that pillar 1 binds time-to-depth and the drop economy, not just income. The offsets are real and countable (four prize rails, free, `PRIZE_POWER` 1.6x, so 4 prizes ≈ 6.4 bought-equivalents: 16+4 ≈ 22.4 bought-equivalent against the old 24) — but that is an argument about STEADY STATE, and the gate is about TIME. **What is still unmeasured: time-to-N-drills for an idle arc against an active one.** Nothing here should be quoted as "the curve is in band". |
| **The A.56 pillar-2 sim cannot resolve a 3pp effect, because its BARE arm reads 102.9%** | A.56 | OPEN — **not a breach, not cleared either**; three arms named | `sim-out/a56-drills.md`. Ceiling-bound BARE = 102.9% (worst seed 103.0%). Above that bias: **longlens 108.7%**, **prize arc+set 105.8% / 105.1%**, **unmaking 106.0%** | The script's first run flagged fifteen "breach candidates" at 102–109%, and the tell that they were mostly instrument was in its own first row: a bay carrying NOTHING read 102.9% of the field's production. The A.42 ledger already names that cause (`applyFieldSize` seats new cells at full cap — charge injected where no window-differencing can attribute it). Subtracting the bare arm's own overshoot leaves three arms still ~2–6pp over, at n=3 seeds, against a named ±10% instrument noise band (A.39 B6). **What IS proved, deterministically and without the sim, is the structural claim**: `drills-a56.test.ts` "no ability can take more charge out of the field than the field held" runs all fifteen at grade VII with regen OFF and asserts `taken + remaining ≤ held`. So none of them can MANUFACTURE charge; what is unresolved is whether the strongest arrangements sit at the ceiling or a few points over the rate measure's ability to say. Resolving it needs the injected-charge counter the A.42 row asks for, not more seeds. |
| **PILLAR 1 for the alloys: worst 1.70x against a ~5x bound** | A.56 | **MEASURED, in band** | `sim-out/a56-drills.md`, POWER-BOUND scenario: worst alloy-vs-bare income ratio **1.70x** (`prize arc+set`), against A.53's shipped worst of 1.74x | The strongest thing this phase can build — a prize chassis carrying two abilities at once — is no stronger relative to a bare bay than the single ability A.53 already shipped. The twelve new abilities span 0.99x (ganglock, lodecall) to 1.63x (everywhen), i.e. they are TRADES rather than a ladder, which is the shape the A.54 mix reading wanted. Drops move the other way for the strongest income arms (arcvein 0.97x, everywhen 0.83x, cinderhold 0.72x), so nothing is strictly better at everything. |
| **A reach-family stroke rolls drops on WEIGHT, not on charge** | A.56 | **HANDLED, with one grandfathered exception** | `strike(..., weightShare)` in `systems/drills.ts`; `rollForDrop`'s fragment/relic rolls scale on `weight` before the charge check | `rollForFragment` and `maybeDropRelic` fire on the drop WEIGHT regardless of how much charge came out, so an ability touching twelve cells per stroke would multiply the material economy twelvefold on a term pillar 2 cannot see — the exact shape of the A.55 guaranteed-ore-roll bug. Every A.56 reach hit therefore passes `DRILL_DROP_FACTOR × share`. **THE ARC IS EXEMPT AND STAYS AT FULL WEIGHT**, because its income was actually measured (1.74x, `sim-out/a53-alloy-rtp.md`) and re-weighting it would silently void that reading. That asymmetry is a known inconsistency, not an oversight. |
| **The A.53 alloy sim was left fitting a field that no longer exists** | A.56 | **FIXED before it could lie** | `scripts/a53-alloy-rtp.ts` `build()` now writes `fits: [{id, grade}]`, not `alloy` | Renaming `DrillState.alloy` → `fits` would have left that script producing FIFTEEN BARE ARMS and reporting every one of them at 1.00x — a harness that cannot fail honestly, and the numbers would have read as "the abilities are all balanced". Caught by grepping the rename rather than by running it, which is the only reason it is a footnote instead of a phase. |
| **THE SET was inert until the sim caught it** | A.53 | RESOLVED — kept here because the failure mode recurs | `pickTarget` in `systems/drills.ts` now scores `charge × residueBite`; regression test "the bay comes BACK to rock it softened" | 28 unit tests passed while emberset was worth exactly 1.00x against a bare bay over six hours: the targeting rule chased the FULLEST cell, so it could never return to the one it had just softened, and an ability whose entire text is "the next bite takes far more" never got a next bite. Every test asserted the mechanism in isolation and none asserted that the bay would use it. Same family as the A.42 finding — a per-part guarantee is not a system-level one. |
| **ONE ALLOY PER DRILL — the bay is a mix, not a setting** | A.53 → **revised A.54** | **BUILT** | `DrillState.alloy`; `drillAbility`/`bayAbility`/`drillsCarrying` in `systems/drillAlloys.ts`; per-drill ALLOY button + THE MIX readout in `panels.tsx`; drill-target row in `AlloyBench`; save v32 migration | A.53 fitted one alloy bay-wide, reasoning that anything per-drill would rebuild A.52's configuration screen. That was the wrong lesson from the right reversal: what made A.52 a chore was that a drill needed SETUP before it worked properly, and here a bare drill works fine. So the decision is now which of your machines gets which ability. **The marks belong to the ROCK, not the drill** — a cell softened by an emberset drill is soft for whatever bites it next — which is what makes assembling a mix worth more than cloning one choice. The v32 migration copies a previously bay-wide alloy onto every drill, so nobody loses what they were running. |
| **Fitting an alloy is a POUR, and the price is a real spend** | A.54 | **BUILT** | `alloyCost`/`slagCost`/`ALLOY_POUR_BASE` in `systems/drillAlloys.ts`; no equip verb in the action union | A.53 charged a flat 20 of the shell's coin — three drill upgrades, for an ability that changes how the grid behaves, and swapping was a free toggle. The price now reads the ability's SIM-MEASURED weight (`def.weight`, set from the alloy-vs-bare table, not from feel), the shell's ordinal on the quench trough's shape, and the number of drills in the pour. Arcvein at Loam is 240 + 4 of each material per drill; alloying a full bay of 24 costs about what the 24 chassis cost, which is the intended pressure toward a mix. There is no free equip: fitting is always a pour, so a swap costs again. Pulling one OUT is free — stopping should never be a purchase. |
| **An unknown mix does not quote its price** | A.54 | **BUILT** — a deliberate withholding | The `priced` guard in `AlloyBench`; the note on `forgeDrillAlloy` | Pricing per ability creates a free scanner if the price is shown before the mix is known: read the number, learn whether it is slag or which ability it is, never pay to find out. So a KNOWN ability shows its cost (you know what you are buying) and an unknown mix says "no telling, until you have made one" and pours at whatever it wants. The one thing that leaks is that a player who cannot afford an unknown pour learns they are short, which is not a recipe. |
| **A MIXED bay does not compound past the best single ability** | A.54 | MEASURED — the reading, not a worry | `scripts/a53-alloy-rtp.ts` `mix` arm; `sim-out/a53-alloy-rtp.md` | The open question the per-drill move created: since the marks are on the rock, an emberset drill softens cells the arc drill beside it then bites, so the abilities could stack. They do not. A round-robin mix reads 1.40x against bare, sitting between arcvein alone (1.74x) and lodecall alone (0.99x) — mixing is a trade between income, drops and softening rather than a strictly-better stack, which is the shape a real decision wants. Pillar 2 still holds (no arm above the field's own production); pillar 1 worst is still 1.74x against a ~5x bound. |
| **ORES IN THE GRID — a denser pocket of what you were already mining** | A.55 | **BUILT** | `content/ores.ts` (4 types, 3 universal + Ferrite's own), `systems/ores.ts` (spawn/dig/open), pocket drawing + dig ring in `ui/face/FaceView.ts`, `__tests__/ores.test.ts` (33), `scripts/verify-ores.ts` (26 driven checks) | An ore raises a cell's CAP and nothing else. `dpsMax = W·H·regen·Y` has no cap term, so **the ceiling cannot move by construction** — you take charge the rock made, concentrated and on a delay. It refuses an ordinary chip AND a sweep, so the only ways in are to WORK it (a hold, `digSec` of attention) or leave it to a drill. The hand takes it clean with whole guaranteed rolls; a drill is faster, leaves 15% in the rock, and gets a fraction of a roll. Spawning is a trickle + a 20% cap + a 60s anti-drought floor, in both scattered and vein patterns. Sim: charge converges at 1.001–1.010× of a zero-ore control while rare drops run **2.06–2.34×**. |
| **Each ore type is a different SHAPE, not four hues of one crystal** | A.55 follow-up | **BUILT** | `OreDef.pattern` (bands / cluster / core / needles); the four branches in `drawTile`; the pixel-sampling check in `scripts/verify-ores.ts` | The first cut drew one faceted crystal in four colours, which at 380px — a tile is ~55px — reads as "that is an ore" rather than as four different things. Now: Fat Seam is flat strata, Blind Glut is round nodules, Heartrot is a bright cracked core, Lodeknot is needles leaning inward. Shape carries the type, so it survives a player who cannot separate the hues. Verified by SAMPLING THE RENDERED PIXELS, not by reading the defs: a 7×7 grid over each tile, thresholded against that tile's own mean, gives four distinct silhouettes (16/34/37/20 of 49 samples lit). |
| **A drill commits to a pocket and finishes it** | A.55 follow-up | **BUILT** — two real abandonment paths closed | `applyFieldSize` remaps `ore`/`oreDug`/`drill.oreCell`; growth will not sprout on a pocket; the drill's release rule is down to one clause | Found by probing the live engine rather than reading it. **Widening the face wiped every pocket on the grid and abandoned every dig** — `applyFieldSize` rebuilt `cells` and left `ore` at the old length, so the next read replaced the array with empties. An upgrade purchase destroyed the contents of the face. The remap is by COORDINATE, because a wider grid renumbers every row. Second: **a vine spreading onto a pocket made the drill let go and bin the work**, fixed at the source (nothing grows on a pocket) so the drill's rule keeps one clause — it releases only when the pocket is gone, i.e. the player opened it first. |
| **The ore reward had to move from COUNT to RARITY, and the sim is why** | A.55 | RESOLVED — recorded as method | `openOre` / `OreDef.depthMult`; the discarded readings head `sim-out/a55-ore-ceiling.md` | Three wrong answers before the right one. Guaranteed rolls for every pocket read **26× the baseline drop economy** at shallow depth — a bay opens ~130 pockets an hour and a guarantee ignores `dropChance` entirely, so pillar 1's material-gate bound was violated while pillar 2 never noticed. Removing them read **1.00×** — ore worth nothing. A flat depth bonus (+15/+45/+90) read **0.96–1.05×**, because fifteen depths is a rounding error at depth 40. The shipped answer: rarity scales with depth (`depthMult`), whole rolls for the HAND (rate-limited by attention, which no upgrade inflates) and a 0.15 fraction for the bay. |
| **A pocket thins the seep while it fills** | A.55 | OPEN — accepted, bounded, documented | `tickFace`; the test "withholds seep while it fills, and pays it all back on opening" | Seep is a cut of OVERFLOW, and a pocket overflows later because it holds more, so an unopened one does briefly thin the idle leak. Found from `progression.test` failing, not reasoned about up front. Accepted because the charge is DEFERRED rather than lost, and the exchange strongly favours the player: seep pays 15% of what it takes, opening the pocket pays 100%. Anyone with a bay recovers it automatically (hunting defaults ON). The sim's no-drill arm reads 0.986× charge — inside noise. Revisit if a shell ever ships without a bay. |
| **Drill routing is one bay-wide switch, and area targeting is NOT built** | A.55 → **A.56** | **SUPERSEDED — built, see the A.56 routing row** | `state.drills.huntOres` is still the DEFAULT; `DrillState.zone`/`priority` override it per machine | The brief offered "pick an area, or set them to hunt ores, or both" and then answered itself — "'Send drills at ores' is the whole feature". This row asked for a reason beyond completeness before building the rest; the reason arrived at A.56, when fifteen abilities made WHERE a machine works a decision rather than a knob. The row's caution was still right and is what shaped the build: routing is opt-in, and every default is the old behaviour unchanged. |
| **The drought floor ignored the ore-chance modifier** | A.55 | RESOLVED — found by the sim | `tickOres` early-out at `chanceMult <= 0` | The sim's "no ore" control arm was seeding **393 pockets an hour** through the anti-drought path, so every comparison against it was meaningless. The fix is also correct engine behaviour: a world whose ore chance is zeroed (by a law, a challenge, or a control arm) has no ore, and a safety net for a quiet face is not a mandate that ore must exist. Exactly the "a sim result is a claim until the harness is verified" rule, caught by reading the pocket count in a column that should have been empty. |
| **The modifier breakdown tooltip was clipped, and had been for a long time** | A.54 live-play | RESOLVED | `BucketInfo` in `ui/components/shared.tsx` (now portalled); pure `tooltipPlacement` + 7 unit tests | Reported from play against "speed bonuses". It was an `absolute` panel inside a `relative` span, which every browser clips to the nearest scrolling ancestor — and every room in this game lives inside `overflow-y-auto`. So the affordance whose entire job is "you can always see where a number comes from" could not show the number it came from. Same fix and reasoning as `Select` (A.37): portal to the body, position `fixed`, clamp to both edges, flip when there is no headroom. Rows now wrap instead of truncating, because a clipped SOURCE NAME is the same defect one level down. |
| **The tooltip's flip-down branch is unreachable through any room** | A.54 | OPEN — a guard, not a defect | `TIP_MIN_ABOVE` in `ui/components/tooltipPlacement.ts`; `__tests__/tooltipPlacement.test.ts` | The face canvas owns the top ~430px of every screen, so no modifier label ever sits in the top band and the flip never fires in play. The guard is right and cheap, but a branch no driver can exercise is a branch nobody checks — this project has shipped several. So the arithmetic was pulled into a pure function with its own tests, and `scripts/verify-alloys.ts` says out loud that it cannot produce the case rather than hovering a mid-panel label and reporting that it flipped. Revisit if a room ever puts a breakdown near the top. |
| **The alloy sim's first two instruments were both wrong** | A.53 | RESOLVED — recorded as method | `scripts/a53-alloy-rtp.ts`; the discarded readings are described at the top of `sim-out/a53-alloy-rtp.md` | Draft one measured dust ÷ dpsMax and reported a BARE bay at 1.18 of the ceiling — the exact A.43 mistake, re-made a phase later, because dust also comes from the field's starting stock and from seep. Draft two measured charge ÷ charge correctly but let `Math.random` run free, so an extra drop shifted the whole downstream stream, growth spawned elsewhere, drills skipped different vined cells, and an arm that touches no charge-path code read 3% apart from bare and was called a pillar-2 breach. Final: seeded LCG reset per arm, three seeds, and the BARE arm's worst seed as the measured noise floor. Verdict — pillar 2 holds (no arm above the field's own production), pillar 1 worst 1.74x against a ~5x bound. |
| **MATERIAL ABILITIES ON TOOLS — the forge, the drills and the abilities become one system** | A.58 | **BUILT** | `systems/toolAbilities.ts`; `TOOL_CARRIER`/`carrierOf` in `systems/drillAlloys.ts`; `matchAllAbilities` in `content/drillAlloys.ts`; `AbilitiesCard`+`BenchLean` in `CastingPanel.tsx`, `ToolAbilityStrip` in `panels.tsx`; `__tests__/tool-abilities.test.ts` (37); `scripts/verify-tool-abilities.ts` (20 driven checks); save v39 | **NOT a second ability system — the tool became a CARRIER.** A.57's twenty-nine abilities already worked on "a thing that carries fits and strikes rock", and a drill was only the first such thing, so `fireAbility`/`applyPlan`/`buildPlan` are reused verbatim and one index (`TOOL_CARRIER = -1`) resolves to the hand instead of the bay. Three things are genuinely new: the ability comes from the **Head, Edge and Sockets** (three materials — the same arity a POUR has, and the signatures were sized against a pour: `{charged:2, brittle:1}` is demanding out of three and near-certain out of seven, so reading all seven parts would have handed a mixed tool the whole list and made stone choice stop mattering); how many it may carry is **SLOTS** (Binding's `modSlots` − 1, plus one per five levels, cap 4 — a first tool carries exactly one); and the meter fills on a **SWING**. Grade is the deepest of the three stones, so re-casting the Head in deeper rock steps every ability on the tool. Discovery writes to the bay's own codex — learning what Slagburst is at the crucible or in the hand is the same knowledge. Measured reach: Loam builds all 5 of its abilities from local rock with **median 1 per build and 82/231 builds granting nothing**, so the stone is a real choice; deeper shells run median 2, max 6. |
| **PILLAR 2 FOR A TOOL THAT EXPLODES** | A.58 | **HOLDS — deterministic proof + matched-saturation sim** | `tool-abilities.test.ts` "no tool ability can take more charge out of the field than the field held" (every ability, grade VII, nothing ticking the face); `sim-out/tool-abilities.md` | THE STRUCTURAL PROOF IS THE ONE THAT MATTERS and it is inherited rather than re-argued: a tool ability resolves to a plan and the plan goes through `applyPlan` → `deps.hit` → **`harvestCell`**, the same call an ordinary swing makes. The `hit` branch is the ONLY place a hand differs from a machine (a drill's bite is an absolute power, a swing takes a fraction of what the cell holds); both end in `take = min(want, held)`. Sim: power-bound the abilities are worth **1.49×** the same tool carrying none (they collect banked regen a bare swing leaves); ceiling-bound all five arms span **0.11pp**; the model-free faucet test reads **1.0012×** a saturated bare arm against a 0.50% instrument. Pillar 1 against the idle player who actually exists (six machines): **0.73×**. |
| **The tool-ability sim's faucet gate was unmatched on saturation** | A.58 | **FIXED — recorded as method, it is A.57's defect one phase later** | `scripts/sim-tool-abilities.ts`, the comment above the faucet block | The first run flagged **1.0057×** — a faucet. It was taking every arm at ≥2 clicks/s and scoring it against a bare arm measured at **200**/s, but bare hands at 2/s read 2.853 against their own 2.935 plateau, i.e. 97%. So an ability arm collecting that last 3% of headroom was compared to a reference it had never been matched to. Identical in kind to A.57's first ceiling-gate defect (a bias sampled in one condition, applied in another). Two fixes: the gate takes only the top rate and asserts the bare arm is on its plateau there, and it compares **produced** against **produced** — harvested plus the change in what the rock is holding, the storage term A.42 asked for, because an ore pocket banks charge above cap and a window that spends that bank reads as having grown it. Corrected: 1.0012×. |
| **`allShells()` is empty until an engine exists — third time** | A.58 | **CAUGHT, by a crash rather than a green number** | `createEngine()` at module scope in `scripts/sim-tool-abilities.ts` | `pickStone()` ran at module scope, `allShells()` returned `[]`, and the sim picked a material id of `''` and died in `materialDef`. Same root as A.56's vacuous reach test, which instead passed by doing nothing for three phases. **The crash is the lucky version.** Worth stating plainly: any loop over `allShells()`/`materialsOfShell` outside a function body is wrong unless an engine has been created first. |
| **The tool does not draw on the bay's POWER BUDGET** | A.58 | OPEN — a deliberate asymmetry, named so it is not "fixed" by accident | `abilityBudget`/`loadoutUsed` read `state.drills.units` only; the tool is bounded by `toolAbilitySlots` | The budget exists because a bay has up to 24 machines and a seat on one is nearly free, so something had to make carrying everything a choice. A tool has 1–4 seats in total and the scarcity is already there; a second limiter would be two dials doing one job. The consequence to watch: a deep player's tool can carry four power-5 abilities that the same player's bay could not all run at once. Pillar 2 is untouched by this (the limit is not what enforces it — asserted by firing a six-ability loadout no slot count permits), but if the tool ever feels strictly better than the bay, this is the dial. |
| **CASCADE and CATACLYSM only read the BAY** | A.58 | OPEN — small, known, low priority | `chainOthers` and `fireCataclysm` iterate `state.drills.units` | A tool seated with Cascade will not chain off another tool ability (there is only one tool, so this is nearly moot), and a tool-seated Cataclysm fires the BAY's abilities rather than its own — which is spectacular and arguably correct, but it was inherited rather than designed. Both are Aleph-only and neither can breach the ceiling (the depth cap and the single funnel are unchanged). Decide deliberately if a later phase gives the player a second tool. |
| **Sockets holding relics/runes/gems, and MODIFIERS** | A.58 | UNBUILT — step 5 of the doc's build order, untouched | `FORGE_design.md` build order steps 5–6; `PART_DEFS.sockets` exists and contributes stats, but nothing seats a relic in it | This phase used the Sockets part as one of the three that decide the ABILITY, which is a different job from the one the doc gives it ("hold relics/runes/gems for their effects"). Those are not in conflict — a socket stone can both decide what the tool reaches for and hold a relic — but the relic/rune/gem tie-in is still entirely unbuilt, and so are modifiers and the per-shell gating of part types (step 6). Named here so the doc's remaining steps are on the record rather than assumed done because the Forge "works". |

| **THE DEEP MODIFIER LIBRARY — thirty-two, four categories, and the OP arc** | A.59 | **BUILT** | `content/toolMods.ts` (32 defs, `ModEffectDef`, `MOD_AXES`, `matchToolMod`), `systems/toolMods.ts` (the two-pass fold, the verb, `tickToolMods`), `ModBench`+`StackTotal` in `CastingPanel.tsx`, `__tests__/tool-mods.test.ts` (43), `scripts/verify-tool-mods.ts` (24 driven checks), save v40 | Fourteen STAT, nine ABILITY, five UTILITY, four COMBO. Applied at the bench by working stone in, on the same discovery grammar as an alloy pour (hint the lean, never the modifier; aim only at what you have already made). **PILLAR 2 IS IN THE TYPE**: `ModEffectDef` names every axis a modifier may touch — reach, splash, ore speed, drops, durability, xp, ability behaviour — and has NO FIELD for yield-per-charge, so a faucet is unrepresentable rather than merely unwritten; `MOD_AXES` is asserted over the data so the guard survives the thirty-third entry. The OP ARC is measured, not asserted: a Loam tool has **2–3 slots** (the 5-slot capstones are literally unreachable), an Aleph one 10–18, and levels add one per five — so an opening tool takes one cheap modifier and a deep levelled one takes fifteen. COMBOS are worth nothing alone and say so: `Resonance` amplifies every OTHER modifier by 1.5×, `Conduction` mends on every firing but wants Self-Mending and an ability, `The Whole Note` wants Resonance and Tempered Intent already seated. Amplification scales the BONUS not the baseline (an amplified 1.4× is 1.6×, not 1.96×) and combos never amplify combos, so two cannot run away together. |
| **PILLAR 2 UNDER A FULLY MODDED OP TOOL** | A.59 | **HOLDS — deterministic proof + matched-saturation sim** | `tool-mods.test.ts` "no modded ability can take more charge out of the field than the field held" (every ability × every modifier at full stacks); `sim-out/tool-abilities.md` | The modifiers land INSIDE the clamps that already existed — reach is floored into `MAX_EXTRA_CELLS` (the 3×3), splash and every ability `share` are capped at a whole cell by `abilityParams` and again by `harvestCell`. So a nonsense stack resolves to "takes all of the cells it reaches", which is the ceiling and always was. Sim, 7 arms × 6 rates × 3 seeds: the CHEATED tool (all 32 modifiers, full stacks) reads **3.94× bare hands at 0.05 clicks/s** and **1.002× at 200/s** — enormously more powerful exactly where the field has banked regen going to waste, and identical to bare hands once the rock is saturated. Faucet test **1.0025×** against a 0.50% instrument. The driven reading is the legible one: a bare Slagburst clears 9 cells for 61.2 charge, a Wider-Blast one clears 25 for 170.0 — **6.80 per cell in both cases.** More rock, not richer rock. |
| **The sim's arm filters were matching on LABEL, and silently excluded the two strongest builds** | A.59 | **FIXED — arms are tagged, and an untagged arm is a type error** | `type ArmKind` in `scripts/sim-tool-abilities.ts`; the gates now select on `r.kind` | The faucet gate filtered `label.includes('+')` and the pillar-1 gate `label.startsWith('tool + 4')`. Both worked until this phase added `THE OP TOOL` and `THE CHEATED TOOL`, whose names contain neither — so the two most dangerous builds in the sim were dropped from the faucet test, which then reported on the third strongest **and passed, with output indistinguishable from a correct run**. Same family as every instrument failure in this ledger: a filter that cannot tell "nothing matched" from "nothing was wrong". Caught by reading the raw table and noticing the cheated arm's 200/s number was not the one the verdict quoted. |
| **A modifier stack could end up over budget and work for free** | A.59 | **FIXED — overflow goes dormant, found in a driven screenshot** | The `PASS 0` block in `modCache`; `whyDormant`'s over-budget branch; the red `— over` in `ModBench` | `applyToolMod` refuses anything that will not fit, so a stack cannot GROW past the budget — but it can END UP past it, because **rebuilding the tool with a weaker Binding stone shrinks the slots under a stack that was legal yesterday.** The screenshot read `10/9 slots` with every modifier cheerfully applied. Overflow is now dormant rather than deleted (throwing away materials the player spent would be far worse than telling them the new Binding cannot hold what the old one did), and it wakes back up untouched when the room returns. The fix then broke two of this phase's own fixtures by silencing what they seated — which is the rule working, and is why the ceiling tests now level the tool until the whole library genuinely fits instead of pretending it does. |
| **`toolMining` ↔ `toolMods` is a deliberate import cycle** | A.59 | OPEN — safe, documented, and the one exception in the engine | The comment above the `modCache` import in `systems/toolMining.ts` | Unlike the wired pairs (`wireFireDeps`, `wireHandHarvest`, `wireParamTune`), which exist because a module-level SIDE EFFECT would run before its dependency initialised, neither side of this pair touches the other at module-evaluation time: `modSlotsOf` is a hoisted function declaration and `NO_MODS` is only read as a default parameter. Both load orders resolve, and the 63-file test suite exercises many. The alternative was a third module holding one frozen object, or a wire whose fallback would be a second copy of `NO_MODS` — and a shadowed twin constant is exactly what A.44 spent a phase on. If a fourth module ever needs to join this knot, split it properly rather than adding to it. |
| **Every mix past Loam makes SOMETHING** | A.59 | OPEN — noted, probably fine, not tuned | Probe over all single- and paired-stone mixes: loam 32/441 make nothing; ferrite→aleph **0** | The matcher ranks by signature demand and takes the best fit, so once a player has reached Ferrite there is essentially no mix that comes out as slag. Discovery tension is therefore about WHICH modifier you get rather than whether you get one — and since materials are consumed either way, "you made Heavy Head again instead of Far Reach" is still a real cost, with `prefer` available for anything already known. Recorded because it is a deliberate difference from the alloy bench (where slag is common) and because if the library ever wants a genuine failure state, this is the number to move. |
| **Sockets holding relics/runes/gems** | A.58 → still A.59 | UNBUILT | `FORGE_design.md` step 5's other half; `PART_DEFS.sockets` contributes stats and decides abilities, but nothing seats a relic in it | Phase 1 used the Sockets part to help decide the ABILITY and phase 2 built the MODIFIERS; the relic/rune/gem tie-in — the doc's "the existing systems plug straight into the tool" — is still entirely unbuilt, as is step 6's per-shell gating of part types. Not in conflict with either phase (a socket stone can decide what the tool reaches for AND hold a relic), and named again so the doc's remaining steps stay on the record rather than being assumed done because the Forge is now large. |

| **MODIFIER LEVELS, SYNERGY TREES AND INSTABILITY — the OP-stacking core** | A.60 | **BUILT** | `content/toolMods.ts` (levels, 3 stabilisers, 9 `SYNERGIES`), `systems/toolMods.ts` (`gainModXp`, `noteSynergies`, `instability`, `steadyOf`), `wireBeforeFire` in `drillAlloys.ts`, `ModLevelBar`/`SynergyCard`/`InstabilityCard` in `CastingPanel.tsx`, `__tests__/tool-depth.test.ts` (35), `scripts/verify-tool-depth.ts` (26 driven checks), save v41 | **LEVELS**: modifiers I–V from the same currency the tool levels on (cells that gave something up), ability-facing ones from FIRINGS at ×40. A level SCALES the vector a modifier already had and cannot rotate it — asserted per-def, which is why levelling needed no separate pillar-2 argument. An ability level is worth a GRADE STEP, so a level-III Slagburst is a 5×5 through machinery that was already proved. **SYNERGIES**: nine pairs that AWAKEN a third thing — Wider Blast + Long Chain → Stormbreaker, Heavy Head + Shatterface → Cleaver. Costs no slots (you arranged it, you did not buy it), sleeps again if either parent leaves, and the hint appears only when you carry ONE half and names neither the other half nor the result. **INSTABILITY**: rises with cost×stacks×level and ability power×level and +12 per woken synergy, falls with `stabilize` modifiers and the tool's own `stability` shape (scale-free, like `toughnessIndex`, so the trade survives depth). Below 40 nothing ever goes wrong; above it, `misfire` caps at 35%. |
| **PILLAR 1 AND 2 UNDER THE FULLY ENGINEERED TOOL** | A.60 | **HOLDS** | `tool-depth.test.ts` "the fully levelled, fully synergised tool still cannot make charge" (every ability × every modifier at level V × synergies awake, asserting a synergy actually woke); `sim-out/tool-abilities.md` | Instability touches ABILITIES only, never the swing — so an unstable tool mines byte-identically to a steady one (asserted), and the worst case is a tool that mines like a tool and sometimes wastes an explosion. A misfire has exactly two outcomes, FIZZLE and WILD, and both strictly remove: driven over 400 firings a misfire took **224.9 charge against a clean firing's 288.0**. There is deliberately no third outcome, because a third outcome is where a compensating upside would go and an upside on a misfire is a faucet in costume. Refires (`depth > 0`) skip the roll so Echoform cannot punish the build that earned it. |
| **The stack readout was promising twice what the tool does** | A.60 | **FIXED — found in a driven screenshot** | `StackTotal` now reads `effectOf` rather than the raw `ModCache`; pinned by "however much is stacked, the EFFECT is clamped where it always was" | The card read **"+16 reach · +205% off each cell"** on a fully stacked tool. Both are lies the ENGINE correctly refuses to tell — reach floors into the 3×3 and splash cannot exceed a whole cell — because the card was summing the fold BEFORE the clamps. The consequence was worse than cosmetic: a player's next stack of reach modifiers would have bought exactly nothing while the readout said it had bought four more cells. It now reports what a swing actually does and says when a term has stopped paying ("reach already at the full 3×3"), which is precisely the information a build needs at that point. |
| **A synergy could wake while mining and tell nobody** | A.60 | **FIXED** | `gainModXp` calls `noteSynergies` on any level-up | `noteSynergies` was called from the bench verbs only. But a synergy wants both parents AT A LEVEL, and levels come from swinging — so the second parent crossing its threshold mid-mine woke the thing with no bench action to hang the announcement on, and the player would have found out the next time they happened to open a panel. The discovery moment of the whole system, arriving silently. |
| **`trueseat` was in the registry, tested, and unreachable** | A.60 | **FIXED — caught by the reach test on its first run** | `needs` changed from `{trueseated:1, tough:1}` to `{trueseated:2, tough:1}` | The first signature was byte-identical to Ferrite's Second Wind, which is deeper and therefore won every tie — so the modifier existed, passed its unit tests, and could not be made by any mix at any depth. Demand THREE outranks every Loam signature, so it is now the answer whenever it fits. **The general hazard: `matchToolMod` ranks by demand then shell depth, so any new signature that duplicates a deeper one is dead on arrival and nothing but the reach test will say so.** |
| **The sim's OP arm builder favours combos over power** | A.60 | OPEN — noted, low priority | The `legal` arm's sort in `sim-tool-abilities.ts`: combos first, then by cost | The legal-build arm fills its 20 slots with expensive combos and runs out before seating much reach or blast radius (measured: "Reach +0.0, blast radius +0.0"). It is therefore a weaker representative of "what a real deep player fields" than it should be — the CHEATED and ENGINEERED arms carry the ceiling stress instead, and they are the ones the faucet gate reads. Worth a better packing heuristic if anyone wants the legal arm to mean something; it currently proves only that a legal build is not a problem, which was never in doubt. |
| **Instability is bounded by a constant, not by a curve** | A.60 | OPEN — deliberate, named | `MISFIRE_CAP = 0.35`, `INST_FLOOR = 40`, `INST_PER_POINT = 0.0022` | Three hand-sized constants, chosen so an opening tool never misfires and a maximally piled one loses about a third of its firings. They have not been through the sim, and a hand-sized constant is a claim like any other (the A.52 draw-ladder row said the same thing about the same class of number). The SHAPE is right and the guarantees are tested — never below the floor, never above the cap, never touching the swing — but if a later pass wants instability to bite harder or softer, these are the three dials and they want a run behind them first. |

| **CAST SHAPES AND EMERGENT TOOL CLASSES — a second and third build axis** | A.61 | **BUILT** | `PART_SHAPES` (22 shapes, 6 head geometries) + `SHAPE_AXES` in `content/forgeParts.ts`; `shapeFold` in `systems/forgeParts.ts`; `reachPattern` in `systems/face.ts`; `content/toolClasses.ts` (5 classes) + `systems/toolClass.ts`; 5 `classOnly` modifiers; `__tests__/tool-shapes.test.ts` (35); `scripts/verify-tool-shapes.ts` (23 driven); `scripts/sim-tool-shapes.ts`; save v42 | **SHAPES**: chosen at the pour, orthogonal to material — the stone sets how big the numbers are, the mould sets where the swing lands. Only the HEAD carries geometry (six patterns: spread/single/block/twin/arc/line); every part's shape contributes an `fx` over the axes the tool already had. Driven: Wide cut `[14,15,20,21]` where Point cut the full 3×3 and Needle took one, from the same marl at the same purity with a byte-identical stat block. **CLASSES**: read off the seven parts, never picked. Coherence is the gate — a set spread across seven shells reads 0.25 and gets nothing at all, whatever its traits say, so the coherence factor that already docked stats now decides identity too. Each class unlocks one modifier nobody else can make; tipping out of the class puts it to SLEEP rather than confiscating it. Nothing lists the five signatures; the tool names the class it landed in, the TRAITS that tipped it (which every material row already prints), and what it opened. |
| **PILLAR 2 UNDER SIX GEOMETRIES** | A.61 | **HOLDS** | `tool-shapes.test.ts` "a shaped, classed, fully modded tool still cannot make charge" (every head × every ability × every modifier); `sim-out/tool-shapes.md` | The patterns move the SAME number of harvests to different rock — `reachPattern` is bounded by `want`, which is `tool.cells − 1`, which is clamped to `MAX_EXTRA_CELLS`, and each pattern is asserted never to return the target, a duplicate, an off-grid cell or more than asked. Sim: at 200 clicks/s the six heads span **0.01pp** and every one reads 3.394 against bare hands' 3.394; faucet test **1.0001×**. And they are not decoration — at 0.05/s they span **11.8%** (Crescent 0.845 against Needle 0.751), which is the other half of the claim. |
| **A Twin head was silently a Point head near the middle of the board** | A.61 | **FIXED — found by asserting no two shapes cut the same rock** | The `near()` guard in `reachPattern`'s twin branch | The far cell was the strike reflected through the centre of the face, which is genuinely elsewhere for most of the board and IS ITS OWN DIAGONAL NEIGHBOUR near the middle: at cell 14 on a 6×6, the mirror is 21, and 21 is one of the eight cells a Point head already reaches. So a Twin swung at the centre cut exactly what a Point cuts, and the mould the player paid extra melt for did nothing. The mirror now has to earn being the twin — inside two steps and it is replaced by the farthest corner. **The test that caught it is the shape of test worth keeping: not "does twin work" but "does any shape duplicate another".** |
| **`currentTool` went on the hot path and had to be memoised** | A.61 | **FIXED** | The parts-keyed cache in `systems/casting.ts` | `assembleTool` derives seven parts, folds ten stats, computes coherence and now a shape fold. That was fine when it was read once a swing. It is not fine now: `modCache` reads it for the slot budget and `toolClass` reads it again for the class gate, `modCache` runs several times per firing, and a firing can happen every swing — the pillar-2 test went from fast to a five-second **timeout** the moment classes landed. That is the profile of a real save under a heavy build, not a test artifact. Keyed on type/material/purity/shape, which is exactly what `assembleTool` reads. |
| **Two modifiers have now shipped unreachable behind an identical signature** | A.61 | **FIXED, and the trap now names itself** | `secondhand` re-signed `{hollow:3, brittle:1}`; new test `no two modifiers share a signature at the same depth` | `matchToolMod` ranks by demand then shell depth, so two entries with the same `needs` at the same depth are decided by array order and the loser can never be made by any mix — while passing every test about what it DOES. `trueseat` hit it last phase (caught by the reach test, which only says "unmakeable"); `secondhand` hit it this phase behind `voidbite`. The new test names both colliding ids directly. |
| **A class-locked modifier is absent from the pool, not refused after matching** | A.61 | **BUILT — a deliberate pillar-5 choice** | The `classOnly` filter inside `matchToolMod`, before ranking | Refusing after a match would have to say "you nearly made a Siege Weight", which is both a tease and a recipe — it would tell a classless player exactly which mix to come back to. So the modifier is not in the pool at all, and the mix falls through to whatever it really is. The cost is that a player cannot discover a class modifier EXISTS by accident; they learn it from the class card, which lists what the class they are in can carry. |
| **Only three of five classes are reachable from a single stone** | A.61 | OPEN — measured, deliberately not tuned | `tool-shapes.test.ts` asserts ≥3 of 5; the driven run landed Siege from graveclay | Siege, Precision and Excavation tip from seven parts of one material; Tempest (`charged 5, springy 3`) and Revenant (`hollow 4, brittle 3`) want trait combinations no single material carries enough of, so they need a MIXED coherent set — several materials from one shell. That is arguably the more interesting build and it is certainly reachable, but it is unmeasured: nobody has confirmed a same-shell mix that tips them while staying above `CLASS_COHERENCE`. Worth a probe before anyone calls the five classes evenly available. |
| **`CLASS_COHERENCE`, `CLASS_THRESHOLD` and `SHAPE_WEIGHT` are hand-sized** | A.61 | OPEN — named, same class as the instability constants | 0.86 / 1 / 0.5 in `content/toolClasses.ts` | 0.86 is roughly "one stray part in seven". The threshold and the shape nudge were chosen so shapes can never carry a class alone (asserted) and a leaning set always tips (asserted). The SHAPE is right and both guarantees are tested; the numbers have not been through a sim, and a hand-sized constant is a claim like any other. |

| **MATERIAL LAYERING (Damascus) — the fourth build axis** | A.62 | **BUILT** | `Part.layers` + `blendOf`/`partMaterials`/`shapeOfPull` in `systems/forgeParts.ts`; `LAYER_WEIGHTS`/`LAYER_MELT_EXTRA` in `content/forgeParts.ts`; `layerDraw`/`canCast` in `systems/casting.ts`; `__tests__/tool-layers.test.ts` (34); `scripts/verify-tool-layers.ts` (27 driven); save v43 | Outer/middle/core at weights 0.50/0.30/0.20 (0.62/0.38 for two), normalised. **The whole design is that a blend produces the same `(traits, magnitude, intensity)` triple a single material does** — so `derivePart` folds the blend and nothing downstream can tell the difference, which is why layering needed no new pillar-2 argument. A trait pulls at its layer's WEIGHT (`rawCharacterPull`), so `keen` in the core of a three-layer part pulls a fifth as hard as `keen` solid — and at weight 1 the weighted form is byte-identical to the single-material one it replaced, one implementation not two. Each layer draws from ITS OWN stone in the crucible queue, outer first, so Damascus is gated on the queue the A.58 phase built rather than on a new resource. Driven: graveclay over hollowamber carried `dense,tough,earthfast,hollow,light` (a part no single stone makes) with the core pulling at 0.38, magnitude 1.1 between the solids' 1.0 and 1.3, and **both stones paid** (7 + 5 melt). |
| **TOOL BALANCE — the fifth axis, and the deadzone is what makes it a trade** | A.62 | **BUILT** | `HEFT`/`BALANCE_DEADZONE`/`WINDUP_MAX` in `content/forgeParts.ts`; `balanceOf` in `systems/forgeParts.ts`; the wind-up gate in `manualChip` and its countdown in `tickCasting`; `BalanceCard` in `CastingPanel.tsx` | Heavy is dense/tough stone, light is light/springy/hollow, summed over every part and every LAYER at its weight — so where the dense stone sits changes the balance, which is the two axes interacting rather than being authored. Heavy buys reach and per-cell take and pays a **wind-up** (max 0.30s); light pays reach and buys wear (0.63× at the extreme) and ability meter (+0.56/swing). **The wind-up exists only on the heavy side, deliberately**: neutral is unlimited clicking, so "faster than unlimited" cannot be sold, and light is paid in durability and meter instead. Measured across the real registry: **48 of 132 single-material tools land exactly EVEN** and are byte-identical to before this existed — that is the no-nerf guarantee, and it is why an emergent axis is allowed to touch every existing tool. |
| **HEAVY AND LIGHT CONVERGE — 1.14% apart at 48.6× the swings** | A.62 | **HOLDS** | `sim-out/tool-balance.md`; `tool-layers.test.ts` "no build of any weight or blend can make charge" | At 200 clicks/s the heavy arm lands **1,000** swings and the light arm **48,568** — and they harvest **3.355** against **3.394**, a 1.14% gap. At 0.05/s, where the wind-up never binds, they read 3.2% apart, so the axis is not decoration. Faucet test **1.0000×**. A layered part reads INSIDE the envelope its two stones set on both the rate measure (0.749 / 0.783 solids, 0.795 blended) and the stat block (**0 of 10** stats escape). |
| **The balance sim was wrong TWICE, and both drafts printed a verdict** | A.62 | **FIXED — recorded as method** | The comment above the click loop in `scripts/sim-tool-balance.ts` | DRAFT 1 advanced the cursor on every attempt including the ones a wind-up refused. A heavy tool refuses ~20 of 21 at 200 clicks/s, so its landed swings stepped `cursor += 21`, and gcd(21, 36) = 3 means it visited **nine cells of thirty-six** over and over while the other twenty-seven sat at cap overflowing. Reported: **heavy fails to converge by 41%**. Entirely the cursor. DRAFT 2 advanced only on a landing — which fixed the heavy arm and broke every other one, because `manualChip` also refuses an empty cell, so **bare hands stopped advancing and hammered one dead cell**: the baseline fell 3.394 → 1.287 and the noise floor went 0.5% → 3.2%. A harness that damages its own baseline still prints a verdict. The third version models the player: blocked = wait (clock passes, arm does not move); ready = swing and move on whether or not that rock had anything left. |
| **The convergence gate is sized on the HEAVY arm, plus a named quantisation term** | A.62 | **BUILT — and it is not tolerance-widening** | The `CONV_TOL` block in `sim-tool-balance.ts` | Gating a 1,000-swing arm on a 48,568-swing arm's seed spread is A.57's mistake in a new costume. The gate is 3× the heavy arm's OWN spread (0.08%, floored at the bare arm's 0.50%) plus **1.5pp for a real, named residual**: the wind-up is decremented on the engine's 0.1s tick, so a 0.28s wind-up spends 0.3s and the arm swings ~7% less often than its constant says. That residual is in the SAFE direction — heavy arrives a whisker under the ceiling, never over — and `WINDUP_MAX` was set to 0.30 (a clean multiple of the tick) to shrink it. |
| **`currentTool`'s memo is now load-bearing for the blend too** | A.62 | OPEN — noted, no action | The parts-keyed cache in `systems/casting.ts`; `blendOf` is called from `derivePart`, `coherenceOf`, `expectedStability`, `balanceOf` and `shapeFold` | A.61 memoised `currentTool` because `modCache` and `toolClass` each assembled the tool. Layering multiplies that again: `coherenceOf` now blends every part, `expectedStability` blends every part, and `balanceOf` walks every layer of every part. All of it is behind the one memo and the suite is fast, but the memo is a single entry keyed on the parts — anything that assembles a DIFFERENT tool in a hot loop (a comparison screen, a bulk preview) will thrash it. Worth a second look if a future phase adds side-by-side builds. |
| **Marl — the starter stone — reads LIGHT, and that is deliberate** | A.62 | OPEN — measured, accepted, recorded | `scripts/balance-preview.ts`: marl (light+springy) raw −0.91; `BALANCE_REACH` 0.30 after re-rating from 0.45 | A starter tool now carries a real trade it did not ask for: ~27% less reach, 27% less wear, +0.54 meter a swing. The reach reduction was 45% in the first cut and was re-rated down after measuring; the opening case is additionally protected because reach floors at one extra cell, so a level-1 marl tool touches the same two cells it always did. The trade only bites once cadence has grown enough for the multiplier to clear the rounding. Accepted as the price of an axis that is genuinely emergent, and stated plainly rather than discovered later. |
| **`HEFT_SCALE` and the balance constants are hand-sized** | A.62 | OPEN — same family as the instability and class constants | `HEFT_SCALE` 1.7, `BALANCE_REACH` 0.30, `BALANCE_SPLASH` 0.25, `BALANCE_WEAR` 0.4, `BALANCE_CHARGE` 0.6, `WINDUP_MAX` 0.30 | `HEFT_SCALE` IS measured — 1.35 saturated every extreme at ±1.00 and made graveclay indistinguishable from protolith, so it was re-rated to 1.7 against the real registry spread (−0.94..+0.94, 48/132 even). The other five are hand-sized against the convergence sim, which confirms the SHAPE (they converge, they are not interchangeable) without pinning the magnitudes. Third phase running with this note; it is starting to be worth one pass that sizes all of them together. |

| **LIVING MATERIALS, TOOL BIOGRAPHY, MASTERWORK — the character layer, kept lean** | A.63 | **BUILT** | `LIVING_SHELL`/`GROWTH_BOONS`/`CRAFT_ODDS`/`MASTERWORKS` in `content/forgeParts.ts`; `isLiving`/`growthFold`/`craftFold` in `systems/forgeParts.ts`; `systems/toolBio.ts`; `matureLivingPart`/`rollCraft`/`growLivingParts` in `systems/casting.ts`; `LivingCard`/`CraftCard`/`BiographyCard`; `__tests__/tool-character.test.ts` (38); `scripts/verify-tool-character.ts` (27 driven); `sim-out/tool-character.md`; save v44 | **LIVING**: only Verdance stock grows, and it grows on the same cells-mined currency the tool levels on — regen-bound, un-tappable. Three maturings max, each dearer than the last (2,200 cells then 1.45-exponent), and each is a CHOICE of three: Runner (+reach), Knitting (self-mending), Supple (steadier + less wear). Driven: 400 real swings put 1,200 cells into a head that wanted 2,200, then the choice appeared and taking Runner read "+1 reach". **BIOGRAPHY**: one record, and four of its seven lines are BASELINES SUBTRACTED FROM LIVE COUNTERS the engine already keeps, so only three needed a hook. A rebuild keeps it and counts itself — a history that reset on a repair would discourage the maintenance the doc's "you never throw it away" is built on. **MASTERWORK**: Poor/Good/Excellent/Masterwork rolled at the pour, 4.1% masterwork measured over 3,000 rolls, and it never touches stats — every tier derives a byte-identical stat block, asserted across all four tiers × all four works. |
| **The Casting Floor's "no RNG" rule is NARROWED, not broken** | A.63 | **RULED, and written into the code** | The comment above `CraftTier` in `content/forgeParts.ts`; `rollCraft` in `systems/casting.ts`; the test "a Poor pour is a normal part with no bonus" | Rule 1 of that module has said "no timing window, no quality roll, no RNG anywhere in this file" since step 2, and the brief asks for a cast-time roll. The substance of the rule is NO PUZZLE, NO FAIL — nothing you pour can be botched — and that still holds exactly: a Poor part has the same stats, the same shape and the same layers as a Masterwork one. The roll only ever ADDS, and most of the time it adds nothing. Recorded here because the rule's WORDING is now narrower than it was, and a later phase reading it should know why. |
| **PILLAR 2 UNDER THE CHARACTER LAYER, and the biography proved as an EQUALITY** | A.63 | **HOLDS** | `sim-out/tool-character.md`; `tool-character.test.ts` "a fully grown, fully masterworked tool still cannot make charge" | Faucet test **1.0001×**, five tool arms spanning **0.01pp** at saturation. The biography is not measured statistically but compared exactly: the same seeded arm with a nine-million-cell history and without reads **identical to the last float** at four rates × two seeds. A tolerance would have been the weaker test — something that should be bit-identical either is or is not. |
| **The `currentTool` memo key was serving a stale tool — and had been since layering** | A.63 | **FIXED — caught by the living tests** | `toolKey` in `systems/casting.ts` | The key hashed type/material/purity/shape only. **LAYERS WERE MISSING SINCE A.62**: a solid marl Head and a marl-over-firstiron Head hash identically, so whichever assembled first was handed back for the other — latent, and invisible because every blend test called `assembleTool` directly. Then `grown` and `craft` landed, which `growthFold` and `craftFold` read off `tool.parts`, and three tests failed at once: a Supple part did not steady, a Trueborn did not steady, a Thrifty repair charged full price. The key is now every field `Part` carries. **The standing rule: adding a field to `Part` means adding it to `toolKey`, or the memo silently serves a stale answer.** |
| **The inertness harness was handing its arm a different GAME** | A.63 | **FIXED — recorded as method** | The comment in the `history` branch of `scripts/sim-tool-character.ts` | To build a "long history" arm the sim also did `collapse.count += 200` and `relics.found += 300`. Those are LIVE COUNTERS with real systems on them (the Core tree, relic and museum bonuses) — so the arm was not "the same tool with a history", it was a save two hundred collapses deep, and it read **1.19× the ceiling**. Reported: the biography is reaching something it must not. It was not. **The unit test made the identical mistake and PASSED**, because one manual chip is too small a window to feel the Core tree — right by luck. Both now vary only the biography's own stored fields, and the test swings twenty times and ticks thirty seconds so the window is big enough to catch a leak. |
| **`craftFold` reported an all-Poor tool as Good** | A.63 | **FIXED** | `bestIdx` starts at −1 in `craftFold` | It floored the best-tier index at `indexOf('good')` so that an untiered part read as Good — but the absent case was ALREADY handled by `p.craft ?? 'good'` one line down, so doing it twice meant an explicitly Poor pour could never be displayed as one. Two mechanisms for one default, and the second one silently won. |
| **Only 3 of the boons are distinct axes, and `GROWTH_MAX` is 3** | A.63 | OPEN — deliberate, the brief asked for lean | `GROWTH_BOONS` (3), `GROWTH_MAX` 3, `GROWTH_BASE` 2,200 | The brief was explicit: "do not build fifteen evolution systems". So three boons, three maturings, one curve — which means a fully grown living tool has nine boons across seven parts and the whole space is `3^3` per part rather than a tree. If a later phase wants living materials to be a real progression rather than a character touch, the shape to add is more boons rather than more stages, because the stage curve is already steep enough that a third maturing is a long way in. |
| **The character-layer constants are hand-sized** | A.63 | OPEN — fourth phase running with this note | `GROWTH_BASE` 2,200 / `GROWTH_EXP` 1.45, `BOON_*`, `CRAFT_ODDS` (20/58/18/4), `EXCELLENT_STEADY` 5, `MASTERWORK_STEADY` 24, `THRIFTY_REPAIR` 0.4 | The masterwork ODDS are verified (4.1% over 3,000 rolls, driven) and the growth curve is verified as monotonic, but none of the magnitudes has been through a sim — the sim proves they do not breach the ceiling, which is a different question from whether they are worth building for. This is now the fourth consecutive phase adding hand-sized numbers to the forge (instability, class thresholds, balance, these). **One pass that sizes the whole forge together is overdue and should probably come before any further forge feature.** |


| **REAL SOCKETS — the doc’s step 5, and the `attunement` stat finally has a consumer** | A.64 | **BUILT** | `systems/toolSockets.ts` (`socketCount`/`socketFocus`/`setSocket`/`socketRow`, three registrars); `wireSocketed` in `relics.ts` + `wireSocketedPowers` in `relicPowers.ts` + `wireEmptySockets` in `casting.ts`; `SocketsCard`/`SocketHeld` in `CastingPanel.tsx`; `__tests__/tool-sockets.test.ts` (35); `scripts/verify-tool-sockets.ts` (39 driven); `sim-out/tool-sockets.md`; save v45 | The two rows below said UNBUILT and the A.63 audit confirmed it: `attunement` was the ONLY one of ten tool stats with no consumer anywhere in `src/`, and the three systems the doc calls "the tie-in" were wired to the LEGACY tool instead. Now the Sockets PART’s own attunement gives **1–5 slots** (marl 1, weepstone 2 in Loam, voidstar 5) and a focus of **1.00–1.28×**, so the doc’s "hollow/charged materials give more" is true by measurement. Nothing is re-implemented — a relic goes through `effectiveAffixes`/`wakingStep`/`activeResonances` and `activePowers`, a gem through `gemDef` + `gemCutMult`, and **THE SOCKET ROW IS A RUNE SEQUENCE**: adjacent sockets are read by `sequencePairs`/`sequenceTriples` against the unchanged `RUNE_PAIRS`/`RUNE_TRIPLES`, so order matters in the row because it already mattered in the alphabet, and a pair found by arranging writes to the same `runes.pairsSeen` codex an etching writes to. Reversible by the shape of the verb: `setSocket(slot, null)` is the same function, nothing is consumed either way, and dissonance is REFUSED rather than burning the runes (an inscription may eat them; a thing you can pull out may not). |
| **THE SOCKET TRADE COST NOTHING TO BUILD — a socketed relic does not wake** | A.64 | **BUILT** | `tickRelics` accrues carry-time for the WORN set only and `toolSockets` deliberately does not touch it; `powerLive` wants Stirring. Test: "a DORMANT relic brings no power, and a socket will never wake it" (120s ticked, charge stays 0) | Sockets are capacity, and capacity bought with a deeper stone is how every axis in this forge works — but it would have been a flat gain if the only cost were a belt slot you got back. Two systems already saying what they said supply the trade instead: a cold relic in a socket gives its affixes, has NO power, and stays cold forever. Wear it to grow it, set it once it has grown. Nothing was added to make that true. |
| **PILLAR 2 UNDER A FULLY SOCKETED TOOL** | A.64 | **HOLDS — and the gate is the structural one** | `tool-sockets.test.ts` "a socket touches no reach, splash or ore term of the swing" (every slot a Mythic yield relic; `effectOf` byte-identical) + "so its affixes are counted exactly ONCE"; `sim-out/tool-sockets.md` | The structural claim is that every socketed contribution leaves the module through `registerModifier` into an EXISTING bucket — no new bucket, no flat grant, no path to `harvestCell` — so the SWING is provably untouched, and the shared pool means nothing is folded twice. Sim: the highest load anywhere in the table is **96.5%** of what the field made, across five arms including one carrying five Mythic +200% yield/regen/cap relics; empty row against full row differ by **0.07%** at saturation. The regen arm’s field really did make **1.14×** the charge, which is a relic doing what a face upgrade does and has never been forbidden — what is forbidden is taking more than the rock made, and nothing did. |
| **44% OF EVERY RELIC AFFIX IN THE GAME WAS INERT, AND HAD BEEN SINCE PHASE 15** | A.64 | **FIXED — found by the socket driver, and it was never a socket bug** | `affixBucketBonus` in `relics.ts`, read by BOTH `relicBonus` and `socketRelicBonus`; test "an affix reaches its bucket even when its id is not the bucket name" | `mintRelic` writes `affixes[key]` where `key` is the AFFIX ID (`hardDrill`, `deepYield`, `fatSeam`) and every reader looked it up by BUCKET NAME (`drillPower`, `dustYield`, `dropRate`). **Fourteen of the thirty affixes differ** — precisely the thirteen Phase 15 added "so a pool is a hunt and not a list", plus `offlineEff` — so those fourteen landed in a key nothing read. **Measured: over 3,000 mints from the `depth` pool, 1,903 of 4,283 rolled affixes (44.4%) were doing nothing.** A relic could roll Deep Yield +28%, print it on its card, and be a decoration. Found because the socket driver asked the live `drillPower` bucket to move and watched it refuse — the socket had inherited the bug verbatim, which is the design working correctly on a broken foundation. **This is a real power increase for every existing save** (the fourteen wake up); pillar-2-safe, because they land in the buckets they were always authored for, and the sim was re-run after the fix. |
| **A WIRED HOOK CANNOT BE TESTED THROUGH AN IMPORTED MODULE** | A.64 | **RECORDED AS METHOD — a harness rule, found by a false failure** | The comment above the `pool` block in `scripts/verify-tool-sockets.ts`; probed directly (`toolSockets.relicIsSocketed` true while `relics.isSocketedRelic` false on the same page) | The driver called `relics.equipRelic` on a module it had dynamically imported and the shared-pool guard did not fire, which reads exactly like a missing guard. It was not: Vite hands a driver’s `import(’/src/engine/systems/relics.ts’)` a SECOND instance whose `wireSocketed` was never called, while the app’s instance — the one the game runs on — is wired. The fix is to go through `engine.dispatch({ type: ’equipRelic’ })`, which is the live path and what a driver should have been using anyway. **Any future check of a `wireX` hook has to dispatch, not import.** |
| **The socket sim’s first draft printed a verdict off a malformed faucet test** | A.64 | **FIXED — recorded as method** | The header comment in `scripts/sim-tool-sockets.ts`; the gate now reads integrated LOAD | Draft one compared each arm’s absolute produced charge against a BARE-HANDS arm and flagged **1.10×** — "SOMETHING BEAT THE ROCK". Nothing had. A relic carrying `regen` (or a power that does, or the rune pair `mol|ash`) genuinely makes the field produce more charge, because `regen` is a term IN `dpsMax` — so the two arms were not hammering the same rock and the reference had the lower ceiling. Exactly A.57’s defect: a bias sampled in one condition, applied in another. Fixed at the root by integrating `cells × cellRegen` per tick, which is the denominator `scripts/sim.ts` settled on at A.42 and needs no bias term at all. Two further tolerance defects went with it: the noise floor was measured in produced charge and applied to load readings, and the convergence gate was comparing arms whose FIELDS differ (a `cap` relic makes every cell hold more), so the spread is now reported with its cause named rather than gated. |
| **The `modifierIntegrity` bucket-cast guard greps SOURCE TEXT, so it cannot tell code from prose** | A.64 | OPEN — tiny, deliberate, worked around | `no bucket name is cast into place anywhere in engine source` in `modifierIntegrity.test.ts`; the parenthetical in `registerSocketModifiers` | The guard is right and it caught a real hand-written bucket list on the first run — fixed properly by deriving the domain from `AFFIXES`, which also means a new affix now extends the socket’s reach automatically instead of falling outside it. It then failed a SECOND time on a comment describing the mistake, so the comment had to be reworded around it. Harmless; noted so the next person who trips it knows why rather than weakening the rule. |
| **The suite has a pre-existing intermittent failure, in two different files** | A.64 | OPEN — **not introduced here; confirmed on the pre-socket tree** | `git stash` + 4 runs at 432fdcb: `confluence.test.ts` "a fresh state has none active" flaked there too. Also seen: `verdance.test.ts` "a cell held at cap sprouts, ages, banks overflow" | Roughly one run in four fails one of these two, and both are unseeded RNG tests. Checked against the stashed tree specifically so it could not be blamed on — or hidden by — this phase. Same family as the A.63 biography-equality test in the row below. Nothing here depends on it; a seeding pass over the RNG-dependent suites would close it. |
| **The A.63 biography-equality test failed on a clean tree** | A.63 → **FIXED A.64** | **FIXED** — the stream is seeded; still an equality, not a tolerance | `swing()` in `tool-character.test.ts` now pins `Math.random` around each arm; 5 consecutive full-suite runs green | The A.63 close-out reported 1395/1395, and the headline claim of that phase — "the biography is proved as an EQUALITY" — was guarded by a test that failed most runs at HEAD, with the two readings SWAPPING between runs. The cause was A.63’s own fix: widening the window to twenty swings plus a 30s tick so a leak would have somewhere to show was the right instinct, and it put ore spawning, growth and the drop rolls on live `Math.random` underneath an exact-equality assertion. The window stays wide and the stream is now pinned. The sim’s version of the same claim was seeded from the start, which is why it held while this did not. |
| ~~**Sockets holding relics/runes/gems**~~ | A.58 → A.59 → **BUILT A.64** | **SUPERSEDED** — see the A.64 row above | — | Named UNBUILT twice, and correctly both times: two phases used the Sockets part to help decide the ABILITY while the doc’s own tie-in stayed unbuilt. Closed. |


| **THE CASTING SCREEN REBUILT AS A TOOL STATION** | A.65 | **BUILT — UI only, `src/engine` untouched** | `ToolDiagram`/`UpperDials`/`LowerDials`/`RackShelf`/`CrucibleBar`/`SeatBar`/`TheStation` in `CastingPanel.tsx`; `scripts/verify-station.ts` (24 driven checks); mockup `The Hollow - Tool Station.html` in the repo root | Eight stacked text cards became a BENCH: the tool in the middle as an assembled pick — head, edge, binding, core, handle, grip in their real positions with gem seats bored into it — labelled by material with leader lines, dials orbiting it (coherence big, class, level ring, instability, balance), a rack shelf you tap to seat, and a two-column crucible/mould. Plain positioned `div`s and one `<svg>` ring; **no canvas**, per the A.50 ruling that these screens are panels and rendered art is not retried here. A part's colours come from `MaterialDef.palette`, which already exists for every material — so a graveclay head and an alephite head are visibly different objects and nothing needed authoring. `git diff --stat`: one file, `src/ui/`. |
| **THE DIAGRAM SHOWS THE WORKING SET, which is the thing a mockup cannot decide** | A.65 | **BUILT** | `workingPart()` — bench part first, built part second; `data-bench="1"` on the diagram; `SeatBar` | The mockup shows one tool with an empty Binding and could not say whether that is your tool or a tool you are assembling. The engine has both (`casting.tool` and `casting.bench`), so the diagram prefers the BENCH and falls back to the TOOL: the diagram is always your tool, and seating a replacement visibly puts it on before you commit. That is what removed the seven-row bench list — the list and the tool were two pictures of one thing. |
| **THE REACHABILITY TEST CAUGHT TWO VERBS LOST IN THE RE-LAYOUT** | A.65 | **FIXED before shipping** | `reachability.test.ts` failed on `benchClear` and `bringToFront`; now `SeatBar` and the `crucible-queue` chips | Deleting the old bench list took the per-slot ✕ with it, so a part could be seated and never taken off; deleting the old crucible took the queue promotion, so which stone pours first became unchangeable — and layering draws outer-first, which makes that a real decision. Both are now station affordances (tap a part → its seat bar with "Take off"; tap a queued stone → it comes to the front). **The test that catches this is structural and cheap: every dispatchable action needs a live call site.** A UI rewrite is exactly the change that quietly strands a verb. |
| **EVERY OUTLINE ON A CLIPPED PART HAS TO BE A `drop-shadow`** | A.65 | **FIXED — three attempts, recorded as method** | The filter chain in `ToolDiagram`; the marker on the label | The "this part is on the bench, not committed" affordance took three goes. (1) `box-shadow` — invisible on the Head and the Edge, because `clip-path` clips the shadow away with everything else outside the shape. (2) `drop-shadow` appended AFTER two black rims — applied correctly, verified in `getComputedStyle`, and still not legible, because a filter chain COMPOSES and the gold halo lands outside a black rim on a pale stone. (3) Gold rim FIRST, and the real signal moved to the LABEL (`▸ bench`, gold), which is text on a dark ground and cannot be clipped, drowned, or out-contrasted by whatever material the player chose. **The general rule: on a clipped element, a border and a box-shadow both draw against the BOX, not the shape.** |
| **An empty bench read 100% coherent** | A.65 | **FIXED** | `measurable` in `UpperDials`; driven — a fresh floor reads `—` / "nothing seated" | `coherence.factor` is 1 for one part and for NO parts, which is right in the engine (nothing disagrees with nothing) and a lie on a dial: a player who had never poured anything was being told their tool was perfect. The dial now refuses to read below two parts. The empty station renders as a dashed WIREFRAME of the seven seats, which is a better first-run screen than the sentence it replaced. |
| **A shape's blurb became hover-only, on a 380px touch layout** | A.65 | **FIXED** | `mould-blurb` under the shape row | The compact mould column reduced the old cast picker's printed shape description to a `title=` attribute. Shapes are a whole build axis (A.61) and hover does not exist on the device this UI is laid out for, so the axis would have been discoverable only to a mouse. Printed as text again. |
| **The graph had zero casting nodes** | A.65 | OPEN — the graph is stale, and the standing rule handled it | `graphify-out/graph.json` `built_at_commit: 147814a`, 2,306 nodes, **0** matching `casting` | Queried first as the working rule asks, found nothing, and fell back to direct inspection — which is the documented behaviour for a stale graph ("a claim, not evidence"). Worth a regenerate before the next phase that would lean on it; nothing in this phase did. |
| **The station's driver fixture minted colliding part ids** | A.65 | **FIXED — the harness, not the game** | `s.casting.nextId = 1000` in `scripts/verify-station.ts` | Eighteen React duplicate-key warnings, and they were real: the fixture wrote parts at ids 1–7 and 100–102 directly into state while `casting.nextId` was still 1, so the next real `castPart` minted id 1 and collided with a part already in the tool. **The engine cannot do this** — it only ever hands out `nextId++` — the harness did it by writing state behind the engine's back. Same family as every "a sim result is a claim until the harness is verified" row in this ledger. |


| **THE MODIFIER LIBRARY HAD A SOURCE ALL ALONG — what it lacked was a WAY IN** | A.66 | **BUILT** | `forgeDiscover`/`modRevealedBy` in `systems/toolMods.ts`; `pointedAtBy` in `content/toolMods.ts`; called from `castPart` and `buildTool`; `ModLibrary` in `CastingPanel.tsx`; `__tests__/mod-discovery.test.ts` (15); `scripts/verify-crucible-library.ts` (27 driven); save v46 | The report was "the tab is empty with no way to fill it" and the first half was right for the wrong reason: `applyToolMod` has discovered modifiers since A.59, but it needs a BUILT TOOL to render at all and is a deliberate trip to a second bench — so a fresh save saw nothing, and a player with a tool was asked to spend stone on blind mixes to learn what a stone was for. So forging is now a SECOND source, not a replacement: every pour and every assembly reads its materials and teaches what they reach for. **It mirrors the ability grammar rather than inventing one** — `syncToolAbilities` reads a build's three rock-facing stones, matches them against the registry gated by `reachedOrdinal`, records to a codex and emits a found-event; this does the same four things one step earlier. Measured pacing: **0 known at game start, 7/40 learnable in Loam, growing monotonically to 34/40 at Aleph**, the last six needing real mixes. |
| **DISCOVERY MATCHES ON TRAIT PRESENCE; MAKING STILL MATCHES ON COUNT** | A.66 | **BUILT — and the first cut shipped inert** | `pointedAtBy` vs `matchToolMod`; test "a single stone points at something — the bug that made pouring inert" | The first wiring used the MAKING matcher and a fresh save could pour all day without the library moving. A single-material part pools ONE of each trait and most modifiers demand two or three, so nothing was ever satisfied — the feature was wired, called, and taught nothing, which a probe caught before it shipped. Discovery now matches on PRESENCE: pour dense stone and you learn dense stone reaches for Heavy Head; you still cannot MAKE Heavy Head until you bring it two. **Knowing a thing exists and being able to make it are different, and splitting them is what turns a blind mix into a goal.** Asserted directly — there must exist a stone that teaches a modifier it cannot alone become. |
| **A TRAIT NAMES ITS DIRECTION, DERIVED FROM THE REGISTRY** | A.66 | **BUILT** | `traitFamilies`/`traitPointsAt` in `content/toolMods.ts`; shown on every trait cut in `StonePicker`; tests "never names an actual modifier" + "derived, not authored beside it" | Which stone made which modifier was blind, and pillar 5 has never meant brute force. A trait now says where it leans — "work it in and it reaches for reach and what it carries" — and never what it becomes. **The families are DERIVED by walking `TOOL_MODS`**, so a new modifier changes the hint automatically and the hint cannot drift from the registry; an authored table would be a second source of truth about the same thing, which this project has paid for twice (the axiom formula, the affix keys). Gated by `reached`, so a Loam player is told what a trait means for the modifiers they could actually make. |
| **THE CRUCIBLE WAS DRAWN AS A QUEUE AND HAS NEVER BEEN ONE** | A.66 | **FIXED — presentation only, the engine already did this** | `CrucibleTub` in `CastingPanel.tsx`; `chargeCrucible`'s `existing` branch and `bringToFront` are untouched | The engine has merged same-material charges into ONE stone since the crucible existed, and has always poured from the front. The UI drew a two-tone bar (molten orange / solid grey) plus a separate row of "queued" chips, which taught a mechanism the game does not have. Now the tub IS the stones: each takes the share of the bar it holds, the leftmost pours next, and tapping any stone brings it forward. Driven: 3 units + 2 units of the same stone stays **one** stone and gets bigger (Marl 12 → Marl 20); three different stones sit as three; tapping the third makes it the front. |
| **THE MELT IS THE MATERIAL — `MaterialDef.palette`, not an orange gradient** | A.66 | **BUILT** | `CrucibleTub`'s per-stone gradient; driven against `getComputedStyle` | Asked for before and it did not land, because the tub painted one hard-coded molten orange for everything. Each stone now paints in its own `[deep, mid, light]` — the same palette the icon generator and the tool diagram use — with the molten share lit and the solid share banked down, so a heating stone and a ready one read as one material at two temperatures rather than as two substances. **Verified against the real palette rather than eyeballed**: the computed `background-image` is asserted to contain a colour from `materialDef(front).palette`, and bringing another stone forward is asserted to change it. |
| **THE STONE PICKER WAS A 158-SCROLL** | A.66 | **FIXED** | `StonePicker` in `CastingPanel.tsx`, replacing the flat `<Select>` | Three cuts, because they are the three questions actually asked: what am I looking for (trait), where is it from (shell), what do I have (default sort is most-held), plus a search box. It filters what you HOLD, so a trait you own no stone of never appears — no locked list. Driven: 60 options → 23 on one trait → 14 with a search. A horizontal-scroll first cut was reverted: **a scroller hides the options, which is the problem the picker exists to solve**, and it also put chips outside the viewport. Wrapped and compact instead. |
| **The overflow check flagged my own scroller, and it was right to** | A.66 | **RECORDED as method** | The `overflow-x-auto` revert in `StonePicker` | The 380px gate failed on three trait chips sitting past the fold of a scroll container. The page did not scroll sideways, so it would have been easy to call the check too strict and exclude scroller children. That would have been the workaround-as-fix the PILLARS working rule names: the chips really were unreachable-by-sight, in a picker whose entire job is showing you the few relevant stones. Fixed the component. |


| **THE MODIFIER TAB WAS EMPTY BECAUSE THE DISPLAY WAS GATED, NOT THE DISCOVERY** | A.67 | **FIXED** | `ModBench`'s `if (!tool) return null` at `CastingPanel.tsx`; `ModLibrary` moved INTO the Modifiers drawer; `scripts/verify-teeth.ts` block 1 | Reported as "forging leaves the tab empty", and the discovery hook was fine — a probe on a genuinely fresh save (no injected depth, no injected stone, 400s of real mining) poured a graveclay head and learned **Heavy Head and Deep Bite**. What was broken is that the LIBRARY rendered inside `ModBench`, which returns `null` without a built tool. So a player who had poured parts but not yet assembled opened Modifiers and found an empty box, with everything they had earned invisible behind the same guard. The library is unconditional now, sits at the top of the drawer, and the drawer's own label carries the count (`Modifiers · 2 known`) so it is visible without opening. The bench below it explains why it cannot install yet instead of vanishing. **The lesson: "the feature does not work" and "the feature cannot be seen" produce identical bug reports, and only one of them is in the engine.** |
| **THE RACK IS SEVEN SLOTS, AND THEY ALL FIT** | A.67 | **FIXED** | `RackShelf` rewritten as a 4+3 grid keyed on `PART_TYPES`; driven — 7/7 inside the viewport, `scrollWidth === clientWidth` | The strip listed every cast part in cast order and scrolled past four. Wrong shape twice: it hid parts behind a gesture, and it organised by when-you-poured-it when the only question is "what do I have for the Head". Now one cell per PART TYPE, always seven, never grows — so it cannot start scrolling later either — with the count and best stone on the face of each, and the parts themselves behind the slot you open. |
| **A STALE `useMemo` MADE THE RACK COUNT ZERO** | A.67 | **FIXED — caught in a screenshot, not by a test** | `byType` in `RackShelf`, now computed inline | The header read "1 cast" while all seven slots read 0. The memo was keyed on `[all]` — the rack ARRAY — and the engine appends with `state.casting.rack.push(part)`, which keeps the same array identity, so it never invalidated. **The driver missed it because it counted SLOTS and not what was in them**; a screenshot caught it in a second. Seven buckets over a short array is nothing to compute, so the memo was buying no time and costing correctness. Standing rule: never key a memo on a container the engine mutates in place. The driver now asserts the count too. |
| **PAIRING IS NAMED AT THE POINT OF PICKING** | A.67 | **BUILT** | `pairsWellWith`/`pairingLine` in `content/toolMods.ts`; shown under the stone picker | `traitPointsAt` (A.66) said where a stone LEANS and left the real question open — a signature is two or three traits, and nothing said which one to look for NEXT. This answers that: *"Put light or springy beside it and something finishes."* It names the TRAIT, never the modifier, so the destination stays discovered while the next step stops being a guess. Derived from `TOOL_MODS` **and** `DRILL_ABILITIES` together, gated by depth, so it can never recommend a pairing the game would not honour. |
| **BALANCE IS A JOB NOW — heavy cracks ore, light sweeps rock** | A.67 | **BUILT** | `BALANCE_ORE` + `Balance.oreRate`/`Balance.job` in `forgeParts`; `balance.oreRate` folded in `effectOf`; `__tests__/teeth.test.ts`; driven through `workOre` | It was a legible trade with nothing to trade FOR: heavy got reach and bite, light got the same swing more often, both converged, and the readout said "heavy" to no purpose. Tied to the two kinds of cell the player already mines instead. **Reach changed sides** — light sweeps more plain rock, heavy takes more of each cell and cracks pockets — so each side does something the other cannot. Driven: the heaviest Loam stone dug **0.0756** against the lightest's **0.0564** in the same 0.05s of real `workOre`, and the dial reads "Cracks pockets 51% faster". |
| **AND THE TWO-SIDED VERSION MADE THE STARTER TOOL WORSE THAN BARE HANDS** | A.67 | **FIXED before shipping — a test caught it** | `oreRate: 1 + BALANCE_ORE * Math.max(0, value)`; test "NEITHER side is ever worse than bare hands at the other's job" | Ore work applied to both sides meant a LIGHT tool dug pockets slower than the hands — and marl, the starter stone, reads **-0.90** balance, so the first tool a player ever builds would have been a downgrade at the first pocket it met. The project holds a standing guarantee that a tool is never worse than the hands, and a trade between two builds must not be paid out of it. Ore is heavy-only now, exactly as the wind-up is a heavy-only cost and the meter a light-only gain. The guarantee is asserted over **every material in the registry**, not spot-checked. |
| **INSTABILITY WAS UNREACHABLE FOR NINETY PERCENT OF A GAME** | A.67 | **FIXED** | `instabilityFloor` (was a fixed `INST_FLOOR = 40`); `INST_POWER_EXP`; `__tests__/teeth.test.ts` | Suspected of being a pointless stat, and the measurement was worse than that: a level-40 tool packed with every modifier that fits read **raw 18 against a floor of 40** — net zero, misfire zero, and it always had been. It only ever bit on a 61-slot level-200 tool carrying ten capstones at level five. The cause is structural — `raw` is bounded by the SLOT BUDGET (a dormant modifier correctly contributes nothing) and the budget grows all game while the floor sat still. **So the floor scales with the tool now**, which makes "you have packed this with the strongest things it can hold" mean the same at Loam and at Aleph. Plus `cost^1.6` weighting, so a capstone is worth more than its slots and there is such a thing as a volatile CHOICE rather than only a volatile amount. |
| **AND THE COUNTER-MOVE COSTS WHAT IT SHOULD** | A.67 | **MEASURED** | Driven at Verdance depth, level 60 | Empty tool **0%** (net 0 under a floor of 28). Packed with the most expensive modifiers that fit: **35%** misfire, net 192. Give up 2 slots of power for True Seat ×2: **21%**. Give up 4 for Dead Hand ×2: **8%**. Give up 8 for The Anchor ×2: **0%**. That is the puzzle the brief asked for — you want the power, and the stabiliser is priced in the thing you wanted it for. Instability still touches ABILITIES only and never the swing, asserted, so it stays off the pillar-2 ledger entirely. |
| **PILLAR 2 RE-MEASURED AFTER REACH CHANGED SIDES** | A.67 | **HOLDS** | `sim-out/tool-balance.md`, re-run | Flipping reach invalidated the A.62 convergence reading, so it was re-run rather than assumed. At 200 clicks/s the heavy arm lands **1,000** swings and the light arm **48,568** — 48.6x — and they harvest **3.355** against **3.394**, **1.14%** apart against a 3.00% gate. Faucet **1.0000x**. And still genuinely different tools: 3.2% apart at 0.05/s where the wind-up never binds. |
| **THE LIGHT SIDE'S EDGE IS ON AN INTEGER, so it rounds away early** | A.67 | OPEN — named, and the panel says so rather than lying | `reachNow`/`reachEven` in `LowerDials`; driven — "2 cells vs 2" on an early tool | Heavy's ore rate is continuous and true at every level. Light's advantage is REACH, which is a whole number of cells, so a 1.28x multiplier on an early tool's single extra cell rounds to the one it already had. The driver caught the dial claiming "+27% more rock a swing" while the swing touched identical cells. The line now reports MEASURED cells and says plainly when the bonus has not bitten yet ("Sweeps wider — but not yet. Levels buy the reach this multiplies."). **The asymmetry is real and not fixed**: light reads weaker than heavy until the tool has levelled enough for the multiplier to clear rounding. Closing it properly means giving light a continuous axis of its own — the SWEEP gesture is the obvious candidate, since it is literally the verb for clearing plain rock — and that is a design decision, not a constant. |
| **Neither balance nor instability is a pointless stat any more — but neither is a headline** | A.67 | OPEN — an honest read, offered for the cut decision | This phase | The brief asked to note anything still reading as screen-noise. Both now change what you build and both are legible, so neither should be cut. What is true is that **both are late-engaging**: instability by design (nothing goes wrong until you chase power) and balance by accident (light's half rounds away early). A player in their first hour sees "even / 0" on both dials and correctly ignores them. If the screen ever needs space back, these two dials are the right candidates to COLLAPSE into the tucked drawer until they read non-neutral — not to delete. |


| **THE FORGE CONSTANTS, SIZED TOGETHER — five open rows closed in one pass** | A.68 | **DONE** | `scripts/sim-forge-constants.ts` → `sim-out/forge-constants.md`; `teeth.test.ts` density block | Five consecutive phases each added hand-sized numbers and each ledgered the same note. Measured together, because two of the four families turned out to be **the same dial**: `BOON_STEADY` (14 ×3 stages) and `MASTERWORK_STEADY` (24) both feed `stabilize`, which subtracts from instability's `net` — so a fully grown Trueborn tool carries **66 points of headroom, 15pp of misfire, 41% of the whole misfire range**. The "character layer" constants were silently an instability re-rate and nobody knew. Ran under a **no-nerf** ruling: a constant could only move in the direction that leaves every existing build at least as good. |
| **`INST_FLOOR_PER_SLOT` 1.6 → 5.0 — the only constant that needed to move** | A.68 | **RE-RATED, sim-sized** | `systems/toolMods.ts`; measured `net`/slot across four depths | Instability is a DENSITY claim — "you packed this tool with the strongest things it can hold". Measured: a **cheap** fill runs 0.8–5.4 net/slot, a **packed** one 10.0–13.7. Those separate cleanly, but a floor rising at only 1.6/slot sat under BOTH once the tool got big, so an Aleph tool filled with the CHEAPEST modifiers available read **26%** misfire against a packed one at 35% — the mechanic had stopped distinguishing the two things it exists to tell apart. At 5.0 the floor sits above every cheap fill and under every packed one: cheap **0/0/0/1%**, packed **10/25/22/35%** across Loam→Aleph. **Raising the floor is strictly a buff** — `misfire = max(0, min(cap, (net−floor)×rate))` — so no existing build got worse, which is what let it through the no-nerf ruling. |
| ~~**Only three of five classes are reachable from a single stone**~~ | A.61 → **CLOSED A.68** | **WRONG — all five are reachable, measured** | `sim-out/forge-constants.md` §B, over all **132** materials | The row guessed and said so ("it is unmeasured"). Measured: **Siege, Precision, Tempest and Revenant each tip from a single stone** (4–5 stones apiece); **Excavation** tips from none and needs a same-shell MIX, which its 3-trait signature `{dense 3, keen 3, tough 2}` makes structural — no material in the game carries all three. So five of five are reachable, one by mix. **113 of 132 stones (86%) tip nothing**, which is the intended shape: a class is built toward, not stumbled into. NOT tuned — under no-nerf, lowering Excavation's signature could flip a build that currently tips Siege into a tie (`classOf` returns NO class on a dead heat), which would take a class away from an existing tool. |
| **Per-shell class availability is lopsided** | A.68 | OPEN — measured, deliberately not tuned | `sim-out/forge-constants.md` §B per-shell table | From a SINGLE stone: Loam reaches Siege/Precision/Tempest, Ferrite only Siege, Cinder and Hollow only Revenant, Glassmere three. A player in Ferrite who wants anything but Siege must mix. That is arguably correct — mixes are the more interesting build — but it is uneven and now on the record with numbers. Same no-nerf reason for leaving it: every fix flips somebody's current class. |
| **The shape nudge can carry part of a class signature** | A.68 | OPEN — named, benign | `scoreClass` in `systems/toolClass.ts`; §B table | `SHAPE_WEIGHT 0.5` × three favoured shapes = 1.5, capped at `want × 0.25`. So a Revenant build needs only **5.5 of 7** trait-points if it also pours all three favoured shapes. That is the nudge working as designed — shapes are meant to lean — but it means a leaning-but-short build tips, which was never written down. |
| **The light side's reach edge is narrower than A.67 ledgered** | A.67 → **CORRECTED A.68** | **Measured across levels** | `sim-out/forge-constants.md` §C | A.67 said light's advantage "rounds away early" and left it open. Measured per level: L1 **tied** (2 vs 2), L10 **+2 to light** (4 vs 2), L30 **+2** (6 vs 4), L60 **+2** (9 vs 7). So the rounding only swallows it at level ONE — from L10 the light tool genuinely sweeps two more cells a swing, for the whole game. The caveat was real and much smaller than stated. Heavy's ore edge measures **1.34×** on a real `workOre` dispatch. |
| **Growth and craft odds are confirmed, and growth is FAST** | A.68 | OPEN — measured, not tuned | `sim-out/forge-constants.md` §D | Craft tiers over 20,000 pours measure **20/58/18/4%** against an authored 20/58/18/4 — exact. Growth costs 2,200 → 6,011 → 10,821 cells, which at a level-30 Verdance tool's 9 cells a swing is about **244 swings to the first maturing** and **2,115 for all three**. That is quicker than "a long stretch of use" implies. Left alone: slowing it is a nerf, and it is the one constant family where the brief's own framing (attachment, not progression) argues for generosity. |


| **THE PURITY MACHINE WAS ALREADY BUILT — the brief asked me to build it** | A.68 P2 | **CONFIRMED BUILT, then extended** | `systems/refinery.ts` (shipped since Ferrite), its own room at `ui/nav.ts:78`, auto-refine presets, a Compendium page, ~14 tests | The audit had already reported this and the brief asked for it again, so it was worth confirming loudly rather than quietly building a second one. Every bullet of the ask existed: feed low-purity + a cost, get high-purity out (3:1, a strict sink); makes shallow stock useful; gated (Ferrite mastery 3 = depth record 30); plain-panel room; discovery via transmute chains. **The one real gap was that nobody had ever sim-verified the faucet claim** — a unit test can say "three in, one out", it cannot say whether a player who refines everything ends up richer. |
| **THE REFINERY IS A SINK, NOT A FAUCET — measured at last** | A.68 P2 | **HOLDS** | `scripts/sim-refinery.ts` → `sim-out/refinery.md` | Three claims, each measured separately. **Units only fall**: 243 → 1 across the full ladder, 0.4% survives, and the loss returns as slag (deferred, not destroyed). **Worth falls too**, which is the claim that actually matters — measured through `derivePart` over the linear stats, the best refined stack is worth **38.2%** of what the raw stock was worth, so refining trades TOTAL worth for CONCENTRATED worth. **And it cannot skip the ladder**: a fully refined stone never beats the worst stone one shell deeper, **0 crossings** across all seven shells, so ruling 1 holds. |
| **`pristine` — a sixth purity band, and the only one you can only MAKE** | A.68 P2 | **BUILT** | `BANDS`/`BAND_RANGES`/`bandOf` in `materials.ts`; `TRAIT_INTENSITY.pristine` 1.45; `PURITY_CEILING` in `systems/forgeParts.ts` | A drop rolls 0-100, so `exalted` [95,100] was both the top of the ladder AND the end of it — a player with a mountain of exalted stock had nothing to spend it on, which is the dead end the Refinery exists to prevent. `pristine` sits at [101,110], **above what the world produces**, so no drop table can reach it (driven: 4,000 rolls at depth 150, best purity seen 100). It keeps the same 0.15 intensity step every other band takes — one more rung, not a new curve. **Purely additive**: no band boundary below it moved, so no held stack changes band, and no migration is needed — proved rather than assumed, with a test that loads a five-band stack and refines it unchanged. |
| **`PURITY_CEILING` is read off the ladder, not restated as 100** | A.68 P2 | **BUILT** | `clampPurity` in `systems/forgeParts.ts` | `clampPurity` hard-clamped to 100, which would have silently thrown the entire new band away at the one place purity is read. It now reads the top of the last band out of `BAND_RANGES`, so the clamp can never fall out of step with the ladder again — the same "derive it, do not restate it" rule the trait hints and the socket bucket domain already follow. |
| **REFINE-TO-TARGET — the whole climb in one act, priced first** | A.68 P2 | **BUILT** | `climbPreview`/`refineTo` in `refinery.ts`; `ClimbRow` in `refinery.tsx`; `refineTo` action | One band at a time is honest and, past the first rung, tedious — and 3:1 compounding is not arithmetic anyone should do in their head to find out what "take this lot to Fine" costs. The plan is quoted before anything is spent (`324 spent → 27 Good, 216 slag back`). **It is not a discount**: every rung is the same `refine` the buttons always called, asserted both in a test and in play (`by hand 72, climbed 72`). |
| **A climb that arrives with NOTHING is refused, not offered** | A.68 P2 | **FIXED — caught in a driven screenshot** | The `got <= 0` guard in `climbPreview` | 3:1 compounding can consume everything and land zero: ninety poor is thirty fair, ten good, three fine, one exalted, and nothing above that. The first cut returned that as a valid plan, so the Casting floor cheerfully offered **"would take 132 of these to 0 Pristine"** and the trough would have drawn a button that spent everything for nothing. Refused at the source, so every caller is fixed at once. |
| **The Casting floor now knows the trough exists** | A.68 P2 | **BUILT** | `RefineOffer` in `CastingPanel.tsx` | The machine has had its own room since Ferrite, and the moment a player wants it is not in that room — it is standing at the crucible looking at a stone they are about to pour and wishing it were better. The stone picker now says so and prices it ("The trough would take 174 of these to 1 Exalted"). Deliberately **not** a refine button: the trough is a place you go, and two rooms that spend your stock is worse than one that knows about the other. |
| **The refinery faucet sim could not fail on its first run** | A.68 P2 | **FIXED — recorded as method** | The `bestTotal` guard in `sim-refinery.ts` | The worth gate took a `Math.max` over ALL rows including the un-refined baseline, so `bestTotal` WAS the baseline and the ratio came out at exactly 100.0% — printing REVIEW over a table that plainly showed worth falling 4150 → 1584 → 566 → 202 → 71 → 24. Same family as every instrument row in this ledger, and caught only because the table was printed beside the verdict. |


| **LEGENDARY PARTS — what you earn is the PATTERN, the stone stays yours** | A.68 P3 | **BUILT** | `content/legendaryParts.ts`, `systems/legendary.ts`, `Part.legend`, `legendBoost` in `blendOf` | The obvious build — a legendary part is a fixed named object made of a fixed named material — is dead on arrival here, for two reasons that are both load-bearing rules elsewhere. **Ruling 1** says only the shell compounds, so a legendary Loam head is beaten by an ORDINARY Ferrite head and MUST be — a fixed-stone legend is trash one shell after you earn it, which is exactly the "throw it away for a better drop" `FORGE_design.md` forbids. **Coherence** reads `shellOrdinal` per part, so seven legends earned across seven shells would be the most incoherent tool in the game and completing the set would PUNISH you. So the earned thing is the pattern: the part arrives real, poured in the best stone the Hold held at the moment, and `recastLegendary` re-pours it in any stone you hold at the ordinary price. The legend is yours forever; the material stays a decision. |
| **A legend is the top of the rules that already exist, not a new set** | A.68 P3 | **BUILT** | `mintLegend`; `craftFold` unchanged; `PURITY_CEILING` | Three things make a legend legendary and the game already knew how to do all three: it is poured at **`pristine`** (the A.68 P2 band no drop table can reach — you cannot cast pristine stock); it carries a **masterwork perk you do not get to choose**, read by the unchanged `craftFold`; and it carries **one bounded magnitude multiplier**, the only genuinely new term. So a legend is "your stone at its absolute best, plus the masterwork you would have had to be lucky to roll" — better than anything castable because it is the ceiling of the existing rules, not an exemption from them. |
| **A legend never jumps a shell — ruling 1 measured, not assumed** | A.68 P3 | **HOLDS** | `scripts/sim-legendary.ts` → `sim-out/legendary.md`; `legendary.test.ts` | The STRONGEST legend of each shell against the FEEBLEST ordinary part of the next — the tightest form of the claim, not average against average. **Worst crossing 0.682** across all seven shells, so a legend is worth about two-thirds of the next shell floor and the descent curve is untouched. Asserted twice: at the constant (`boost <= LEGENDARY_BOOST_MAX` 1.6, well under `SHELL_STEP` 6.0) and at the derivation, because a bound nobody measures is a bound nobody keeps. |
| **And it is worth chasing: 1.49x the LUCKIEST pour in the same stone** | A.68 P3 | **HOLDS** | `sim-out/legendary.md` §1; driven at `verify-legendary.ts` §3 | The honest comparison is not the average pour — it is exalted stock (100, the highest a drop rolls) that ALSO happened to come out masterwork. A legend beats that by **1.488x**, identically at every depth, because the gain is multiplicative on magnitude and therefore does not decay. |
| **Pillar 2: a fully legendary tool leaks nothing** | A.68 P3 | **HOLDS** | `sim-out/legendary.md` §3; `legendary.test.ts` | Measured as **clamp containment**, and the sim says so out loud rather than implying a charge integration it does not run: a part has never had a yield term, so everything a legend touches arrives at the face through `effectOf`'s three clamps. Measured at the strongest stone in the game, where the splash clamp is **already binding at exactly 1.000** — which is what makes it a test rather than a restatement. A legend buys reach; the face still holds `W x H x regen`. |
| **The idempotence key is the DEED, not the holding** | A.68 P3 | **BUILT** | `state.casting.legends`; `legendary.test.ts` | The obvious key is "am I holding one", and it is a material printer: melt a legend down (or salvage it) and the next one-second beat hands out a second copy, forever. Prize drills can key off the granted object because a drill cannot be destroyed; a part can. And a legend earned with nothing usable in the Hold is **deferred, not lost** — `earned` is a pure read and nothing is written when the pour cannot happen, so a later beat pays it. |
| **`Part.legend` is in `toolKey`, and the re-pour is the case that proves it** | A.68 P3 | **BUILT** | `toolKey` in `casting.ts`; driven at `verify-legendary.ts` §5 | The standing rule (adding a field to `Part` means adding it to `toolKey` or the memo silently serves stale data) has a real consumer for the first time: re-pouring a legend that is currently IN the tool must re-derive on the spot, or the live tool keeps mining as the old stone. Driven end to end — `marl → ironbloom`, and `currentTool` re-read as ironbloom rather than the memo's copy. |
| **Pillar 5 on a list of seven: show the DEED, withhold the NAME** | A.68 P3 | **BUILT** | `Legends` in `CastingPanel.tsx` | "Never show a locked list" forbids a grid of grey padlocks. It does not forbid telling a player what to go and do — and a legend whose requirement is invisible is not mysterious, it is unfindable, since nobody stumbles into "fell three Floor Wardens" by accident. So an unearned row shows its **requirement** and withholds its **name and its line**, which are the parts worth arriving to. You know there is something at Loam 120; you do not learn it is called The First Bite until you are standing there. |
| **The legend mark had to be inline, not its own row** | A.68 P3 | **FIXED — caught in a screenshot** | `diagram-legend-*` in `CastingPanel.tsx` | A rim does not survive the parts' `clip-path` (three attempts, already ledgered), so the mark has to be text. A NAME on its own line was worse: the diagram labels sit at fixed tops 34px apart, so "✦ The First Bite" pushed into the Edge's slot and ran off the left of its 66px box. One gold star costs no height and cannot overflow; the name lives in the title and in the Legends drawer, which have room for it. |
| **`face-resilience` was an unseeded gate, and an unseeded gate is not a gate** | A.68 P3 | **FIXED** | `beforeEach` seed in `face-resilience.test.ts` | It chips a live face, and chipping rolls drops, crits and ore formation off the shared `Math.random` stream. Alone it passed 11 for 11; inside the full suite it failed intermittently, and **the failing line MOVED between runs** — once on the upgrade being affordable, once on the chip landing at all — which is the signature of a shared stream rather than a bug in anything the file guards. It had stopped being evidence about the invariant it protects, so it is pinned to a constant. **It also cost a checkpoint**: a commit went out on a red run because the gating shell chain matched on the word "Tests" instead of on the result. |
| **`confluence.test.ts` remains intermittently red — pre-existing, not this phase** | A.68 P3 | **OPEN** | 10 of 11 full runs green; the red was `a fresh state has none active` | Same unseeded-RNG family as the one fixed above, in a file this phase does not touch, and already verified flaky on an earlier tree by stashing. Left alone deliberately rather than swept into an unrelated phase — but it is the next one to seed, and it is the reason a green run here is reported as "10 of 11" rather than as "green". |


| **THE REACTION BENCH WAS NEVER BROKEN — the search space was** | A.69 | **FIXED** | `benchReading`/`scentOf`/`reactiveMaterials` in `systems/refinery.ts` | Reported as "0 known, 2497 run, nothing happens". Every part of it worked: a known pair fires correctly on the live path. **Measured: 19 valid pairs among 12,403 — one in 653** — and worse than that reads, because the design rule in `content/shell2/chains.ts` is that every chain consumes a material with ZERO other consumers, i.e. precisely the stones nobody stocks, and several then take `bindingclay` which is itself an OUTPUT. Two thousand pulls with no feedback is not experimenting, it is a slot machine, and the player correctly concluded the machine was broken. **The bench now reads ONE stone at a time** — whether anything in the world wants it, never what or with what — narrowing the field **21x, to 1 in 31**. Pillar 5 intact: the pair is still yours to find. |
| **ORE WAS A TAX, AND THE SUPPLY CEILING PROVES THE FIX IS SAFE** | A.69 | **FIXED** | `digSec` in `content/ores.ts`; `scripts/sim-ore-time.ts` -> `sim-out/ore-time.md` | Reported as "takes too long, I just leave them" — which was the RATIONAL play: taking every pocket the field produced cost **28.0 minutes of every 60** holding a single cell, 47% of the hour, during which you mine nothing else. Now **8.7 min/hr (14%)**, dig times 8/12/14/16 -> 2/4/6/5. **Pillar-safe by structure, not by tuning**: `tickOres` rolls `ORE_SPAWN_CHANCE` against a cap, so pockets-per-hour, charge-per-pocket, guaranteed rolls and the depth bonus are all fixed and `digSec` appears in none of them. A faster dig moves the player TOWARD a pre-existing bound; it cannot move the bound. Not a buff — the removal of a tax that stopped players collecting a ceiling the design already set. |
| **THE SEASONING — stats move with use, per stat, at their own rate** | A.69 | **BUILT** | `systems/toolSeason.ts`; applied in `toolEffect` and `wearPerUse` | Reported as "growth is flat", and it was: `toolLevel` bought grants, `wear` ran down, and the ten stats a tool HAS were fixed from the moment it was assembled. Now a signed per-stat drift on its own saturating curve — **the edge goes off (bite -5%, cadence -3%, hard-capped by `SEASON_DULL_MAX`) and the handling comes in (control +18%, durability +22%, arriving slowest and largest)**. A SHAPE, not a bonus: 8 distinct magnitudes, 6 up and 2 down, driven. And "the more you use and break it, the slower it breaks" is `wearResist`, reading TWO histories — cells worked and mends survived — each saturating, capped together at 45%. **Pillar 2**: applied to `tool.stats` BEFORE `effectOf`, so every term still passes the same three clamps. |
| **LIVING GROWTH: three faults wearing one coat** | A.69 | **FIXED** | `GROWTH_BOONS` (8), `boonsFor`/`boonCost`/`boonNumbers` in `systems/forgeParts.ts` | Reported as "all the same rate, same options, no info". All three were literally true and each needed its own fix. **The pool WAS three** and nothing filtered it -> eight boons, offered three at a time, filtered by part type and by the stones own traits (a dense living core sees Thickening, a grip sees Grasping): **5 distinct offers across one tools 7 parts**. **They all cost the same** -> each carries a `pace` (0.8x to 1.5x), and `ready` is now the CHEAPEST offer rather than the base figure, or the pace would be a lie the panel tells. **Nothing printed a number** -> `boonNumbers` exports every magnitude and the button shows it beside its cell cost. |
| **And two of the eight were authored-unreachable, caught before shipping** | A.69 | **FIXED** | `wants` traits in `content/forgeParts.ts` | Seeking and Stillness were pointed at `magnetic` and `earthfast`, which are SHELL traits **no Verdance stone carries** — so two of the eight boons could never be offered by anything. Found by enumerating every (material x part x band) offer and counting, not by reading the table. Re-pointed at traits the living shell measurably has (springy 9, charged 8, keen 3). All 8 now reachable, 13 distinct offers. Exactly the failure the standing rule warns about: a thing that is individually correct and reachable by nothing. |
| **SOCKET PICKERS SAID THE ONE THING THAT DOES NOT VARY** | A.69 | **FIXED** | `PickRow`/`runeWouldSay` in `CastingPanel.tsx` | The relic picker was a `<Select>` of `name . waking step` — "Uncommon depth . Dormant", forty times. Now ROWS carrying the real affixes at real magnitudes, rarity and waking state, sorted awake-first, **searchable by effect** so "which of these gives me drill bite" is a question you ask the box. Gems already had effect text. **Runes have no solo effect at all** — the grammar is entirely adjacency — so a static line was never available; the row instead previews what that rune WOULD say next to what is already in the row, via the same `sequencePairs`/`sequenceTriples` the live readout uses. Unnamed pairs read back as their own key and are dropped, so it cannot become a printout of the codex. |
| **NEGATIVE RELIC AFFIXES HAVE ALWAYS MINTED AT EXACTLY ZERO** | A.69 | **FIXED — found by the picker** | the magnitude guard in `mintRelic`, `systems/relics.ts` | `affixes[key] = Math.max(affixes[key] ?? 0, mag)` was there to keep the better roll when a key comes up twice. `descendCost` (-0.02) and `shortStair` (-0.035) are cost REDUCTIONS, so `Math.max(0, negative)` is **0** — both have always minted at zero and done nothing, and both sit in the `depth` pool that every shaft relic rolls from. Now keeps the biggest by MAGNITUDE, preserving sign. Same family as the A.68 `affixBucketBonus` bug, and found the same way: by making the panel print the real number instead of a name, at which point "0.0% Cheaper descent" is impossible to miss. |
| **Instability and balance were stats with no stated purpose** | A.69 | **FIXED** | `InstabilityCard`/`BalanceCard` in `CastingPanel.tsx` | Both reported as "unexplained". Instability printed a number, a bar and "Mostly X" and never said what a misfire COSTS — it now says it in swings per hundred, itemises what is spending the headroom as a priced ledger, and names the actual stabilisers the player KNOWS (derived from the library, not restated) plus the boons and craft tiers that steady it. Balance led with mechanism (cells, splash, windup) and never said the sentence the mechanic is about — it now opens with **BUILT FOR ORE / BUILT FOR ROCK**, says how to swap, and names what in the stone is driving it. |
| **Sticky section headers — the collapse control comes with you** | A.69 | **BUILT** | the `<summary>` in `Drawer`, `CastingPanel.tsx` | Scroll deep into a long open section and the only way to close it was back at the top. A `<summary>` IS the collapse control, so making it sticky solves it with no new control and no state to keep in sync. Opaque background is load-bearing — a transparent sticky header lets the text underneath read through it. Driven: the section scrolled 663px and the header did not move. |
| **Two harness bugs, both of which would have passed on a broken game** | A.69 | **RECORDED AS METHOD** | `scripts/verify-playtest-a69.ts` | (1) The sticky check called `window.scrollBy`, and **the window does not scroll** — the app scrolls an inner container, so it moved nothing and would have passed on a header that was not sticky at all. It now walks up to the real scroller and asserts it moved. (2) The bench check read `CHAINS` through a `page.evaluate` dynamic import and got **0 chains**: that is MODULE-level state filled at `createEngine()`, and the import resolves to a second module instance. Every other driver gets away with it because it only calls state-passing helpers. Also: an assertion demanded a sticky header MOVE UP, which is what a non-sticky header does — the pass condition was inverted, and only the printed detail caught it. |


| **MODIFIER LEVELS WERE A SINGLE SITTING** | A.70 | **FIXED** | `MOD_XP_BASE`/`MOD_XP_EXP` in `content/toolMods.ts` | Measured the rate rather than guessing it: an active player at three clicks a second banks **25,486 cells an hour**, and the shipped 900/1.7 curve maxed a modifier at 9,504 of them — **22 minutes**, and the report ("5 min") was if anything generous, because reach credits more than one cell per chip. Re-rated to 8,000/3.25: **L2 19 min, L3 3h, L4 11h, L5 28h**. The EXPONENT does the work rather than the base, deliberately — a curve merely scaled up would have pushed the first level out of a session too, and the first level is where a player learns the ladder exists. |
| **DURABILITY HAD STOPPED EXISTING, and the fix is the flavour text enforced** | A.70 | **FIXED** | `MEND_IDLE_SEC` in `systems/toolMods.ts`; `ENDURANCE_CAP` in `systems/toolMining.ts` | Reported as "Unbreaking 5 + auto-regen, tools never break". Measured: a stacked veteran build mended **1.658 wear a second against 0.001 spent** — outpacing hard use by over a thousand times — and reached **471,891 swings against a bare 7,108** before mending was even counted. Two fixes, and the first one is just the modifier meaning what it already said: Self-Mending reads "it puts itself right WHILE YOU ARE DOING SOMETHING ELSE", and it was doing it mid-swing. It now waits `MEND_IDLE_SEC` past the last swing — mining hard genuinely wears the tool, and the pool comes back while you are at the Forge or away, which costs an idle player nothing (pillar 1). Second, the BUILD multiplier on swings is capped at `ENDURANCE_CAP`, so stacks stop compounding. Result: **0/s while mining, 1.658/s once put down, and 13.6h of continuous hard use before the strongest build gives out** against 39 minutes bare. |
| **THE HARDNESS WALL IS A PRICE NOW, NOT A DOOR** | A.70 | **CHANGED — a deliberate contract change** | `UNDER_TIER_FARE` in `systems/depthSys.ts` | `doDescend` refused outright: "a Tier II tool is needed". The brief forbids it ("do NOT require a specific tool to progress"), and the pillars agree twice — pillar 1 wants a path for both players, and the standing rule against gating a structural unlock behind the wall it exists to cross is the same shape. Being under-tooled now costs `UNDER_TIER_FARE` per tier short, compounding: one tier under is a real but payable premium, three tiers under is possible and obviously stupid. A better tool still buys bite, reach, ore speed — and now a cheaper stair — it is simply not a locked door. **Two tests asserted the old refusal and were rewritten to assert the new contract**, which is the honest move when an invariant is changed on purpose rather than broken by accident. |
| **The modifier menu listed names and never said what any of them DO** | A.70 | **FIXED** | `modEffectLine`/`modEffectBits` in `content/toolMods.ts` | "Long Arm, Heavy Head, Lucky Seam" with a source and a slot count — nothing a player could decide anything from. Every modifier has ALWAYS carried a typed `fx` block; nothing new is authored, the menu simply renders the block the engine already applies, so the two cannot drift. Driven: 40 known modifiers, 40 effect lines, each a number ("+1 cells reached per swing"). |
| **The instability advice was five internal terms in one sentence** | A.70 | **FIXED** | `instability-what`/`instability-how` in `CastingPanel.tsx` | It read "seat The Anchor, grow a SUPPLE or STILLNESS boon, pour EXCELLENT or TRUEBORN parts". Now: what a misfire costs in plain words ("roughly 35 swings in every 100, an ability goes off in the wrong place, or does not go off at all — nothing else is affected"), a priced ledger of what is spending the headroom, and three numbered actions that each name something findable. **A proper noun the player has discovered is not jargon** — the brief asks for exactly that ("say plainly use X, with X being something the player can find") — so "You know The Anchor (worth 55)" stays and the shouted craft-tier ids go. My first cut of the verify check had this backwards and flagged the fix as the defect. |
| **THE WORKBENCH IS GONE — and it was not purely redundant** | A.70 | **STRIPPED, with two survivors re-homed** | `CraftWorkbench.tsx` deleted; rune practice → `glassmere.tsx`, gem fusion → `refinery.tsx` | Asked for as redundant with the Casting station, and for TOOLS it was: carve/cut/cast were the old route and the crucible plus the station do all of it. But `reachability.test.ts` failed the moment the file was deleted and named **six** orphaned actions, not four — the room also carried rune PRACTICE and gem FUSION, which casting does not replace. Deleting the room would have silently deleted two working features. Those moved to the rune wall and a new gem bench in the Refinery; the four staged-craft verbs were retired from the action union so the compiler enforces it. **This is precisely what that test exists for** — a UI deletion is a reachability change, and nothing else would have caught it. |
| **The alloy bench moved to the Drills — with two stale pointers behind it** | A.70 | **MOVED** | `AlloyBench` exported from `ForgePanel.tsx`, rendered by `DrillsPanel`; `openAlloyBench` in `store.ts` | An alloy has never touched a tool: it decides a DRILL ability and is poured AT named drills, and the bay had to send players to another room to reach the bench this room is about. Moving the component was the easy half. **Two things still pointed at the Forge**: the bay own "how do I get one" step 1 read "Go to the Forge alloy bench", and `openAlloyBench` — which every ALLOY button on every drill card routes through — still set `tab: forge`. A move is not done when the component renders; it is done when nothing still points at where it used to be. |
| **A bare `/* */` in JSX CHILD position is a text node, not a comment** | A.70 | **FIXED — caught in play** | `ForgePanel.tsx` | The two notes explaining what had been removed from the Forge were **printing on screen**, so the room the phase had just cleaned up displayed the words "Carving", "Cutting" and "alloy" as body text. Caught only because the driver greps the rendered page for those exact words and reported them as still present — a static read of the diff would have shown two comments. The same pattern added elsewhere this session sits inside parenthesised `return (` expressions, where it really is a comment; only the JSX-children ones broke. |


| **THE BENCH READS THE PAIR NOW — and A.70 shipped this wrong** | A.71 | **FIXED** | `benchReading`/`PairRead` in `systems/refinery.ts` | A.70 gave two SOLO readings: "both of these want something", meaning only that each stone appears in some chain. It read as "this pair will work", so a player who trusted it poured two unrelated stones with confidence and got slag — the same lottery as before with a confident voice on top. It answers the real question now: do THESE TWO make something. **What this costs pillar 5, said plainly**: it gives up "which pairs react" as a thing to discover, and keeps the half that was ever worth discovering — WHAT they make, still found only by pouring and still the only thing the Codex records. The fun was never in the guessing. |
| **FIVE CHAINS FOR THE SHALLOW BOARD — and why they are not orphan-fed** | A.71 | **BUILT** | `content/shell2/chains.ts` | Measured: a Loam+Ferrite player could fire **3 of 19** chains; twelve wanted Verdance-and-deeper inputs and three more wanted `bindingclay`, itself an output. The founding rule is that a chain pulls an ORPHAN back into the economy — but the audit says the only orphans left in these two shells are five alloy CASTINGS (crafted, never dropped) and Starmarl, which rolls **11 times in 9,000**. None can carry a board. So these five earn their place by the rule's OTHER route, the one the test already accepts: every one REACHES UP a rarity gate, turning stone that drops in the hundreds into stone the depth gates withhold. Same job — reach — by the arm actually available. Verified against a hold built from REAL DROPS rather than hand-stocked: 27 materials, 351 pairs, **3 fireable → 8**, all firing through the live dispatch. |
| **THE FORGE TAB IS RETIRED** | A.71 | **RETIRED** | `nav.ts`, `TabId` in `store.ts`, `ForgePanel.tsx` → `toolShelf.tsx` | Once casting moved to its own station (A.68) and the alloy bench moved to the Drills (A.70), the room was an archive of the legacy tool system plus one unrelated bench — and an archive does not need a door in the nav. The tool shelf folded into the **Refinery**, which is already the room where tools are broken down, so equipping, socketing and salvaging stopped being split across two rooms. **`forge` was removed from the `TabId` union rather than left dangling**, so any straggler that still tries to navigate there is a compile error rather than a blank screen. |
| **Gear became its own room, beside the Bestiary** | A.71 | **MOVED** | `GearRoom` in `combat.tsx`; `gear` in `nav.ts`/`systemCopy.ts` | Gear is armour and was never a forging bench; the only reason to wear any of it is what is down the hole, so it sits with the Bestiary. The retired room's copy was SPLIT rather than deleted — the tool half is the Refinery's business, the gear half got its own entry with its own nudge. Also caught in the screenshot: the bench header still read **"THE OTHER BENCH — gear"**, a Forge-relative name with no first bench beside it any more. |
| **Five confluences named `forge`, and the right fix was NOT to relabel them** | A.71 | **FIXED** | the mechanics set in `confluence.test.ts` | Retiring the tab broke `every system a confluence names really exists`. The tempting repair was to repoint all five at `casting` — and it would have been wrong: every one of them reads `state.forge.tools`, its alloys and its runes, i.e. the forged TOOL, which is still a real mechanic and was never a room. `forge` joined the mechanics allow-list beside `gems`, `combat` and `weather`. A name that resolves to nothing is not automatically a name that should point somewhere else. |
| **Nothing was silently dropped, and it is driven rather than asserted** | A.71 | **VERIFIED** | `scripts/verify-retire-forge.ts` | The Workbench taught this one phase ago: a UI deletion is a reachability change. Every verb the retired room dispatched — `equipTool`, `discardTool`, `socketGem`, `bulkSalvage` — is fired through the live path and checked to still land, on top of the suite's own reachability test. 13 driven checks; the nav is clean, the shelf equips, the gear room renders. |

| **EVERY CHALLENGE SEAL IS PERMANENTLY FALSE — the challenge layer (§20.2) is plumbing with no content and no door** | A.72 | **OPEN — diagnosed, deliberately NOT fixed** | `registerChallengeLaws` in `laws.ts:143` — **exported, zero callers anywhere in `src/`**. `challengeLaws` (`laws.ts:141`) is therefore an empty Map, so `activeLaws()` returns `undefined` and `sealed()` returns false for all 14 seals. Separately: **nothing ever assigns `spiral.activeChallenge`** — grep for `activeChallenge =` outside tests returns only `null` initialisers (`spiral.ts:220`, `migrations.ts:331`); there is no `startChallenge`/`abandonChallenge` action in `GameAction` | **Two independent holes, either one alone sufficient to make the layer inert.** (1) No laws are registered, so even a running challenge would seal nothing. (2) No code path starts one, so no challenge can run. 9 seals are read at 14 live call sites (`sealHand`, `sealKiln`, `sealTools`, `sealWiden`, `sealDrops`, `sealCollapse`, `sealOffline`, `sealPurity`, `sealGovernor`) and every one of those guards is dead code today; `challengeNum` is in the same position for `regenMult`/`heatRateMult`/`depthCap`. **How it was found:** cutting SKIM (A.72) broke a test asserting "The Unattended seals a technique" — it had been passing because it fired Skim on a fresh engine, where the seep pool is 0 and the verb refuses with *"Nothing worth cupping yet"*. The seal was never exercised. Swapping the assertion to Poleshift, which has no such precondition, made it fail and exposed the hole. That test is deleted with the reason recorded in `progression.test.ts` rather than re-pointed at another refusal, which would have put a green tick back over the same gap. **NOT FIXED BY RULING** — this is §20.2's ten rule inversions, a content-and-UI phase (author the ten law sets, a start/abandon action pair, a challenge room, and completion tracking), not a line in a verb cut. **The guards themselves are correct and should be left alone**; they are waiting on the layer that switches them on. |
| **HOLD (the Roots shop fork) IS DEAD ON THE LIVE PATH — the Collapse wipes the levels it is counted from before it reads the floor** | A.74 | **OPEN — diagnosed, deliberately NOT fixed (scoring pass)** | `collapseSys.ts:131` zeroes `upgrades[roots]` (`retained` is 0), `clampPacked` at :136 then zeroes `shop.packed[roots]`, and `resetCompaction(state, holdFloor(state))` at :168 reads `holdFloor` AFTER both — so it is 0 at every cap. Shown through a live `dispatch({type:'collapse'})` in `scripts/verify-fork-score.ts` check A: roots 40/packed 40/compaction 26 in, 0 of 36 cells holding anything out | **An ORDERING bug, not a missing feature** — check B shows the mechanic is sound the moment the floor is read before the wipe. **How it was found:** the A.74 HOLD-cap sweep returned SEVEN IDENTICAL ROWS (caps 0 through 26, same depth, same drops, to the digit), which is a null effect, not a small one. `shopFork.test.ts` covers HOLD by calling `resetCompaction(s, holdFloor(s))` DIRECTLY and never through `doCollapse` — the exact shape of the working rule *a test that a function works is not a test that anything calls it*. **NOT FIXED BY SCOPE:** what survives a Collapse is the reset ladder, and A.74 was ruled scoring-only with the ladder in scope for measurement alone. The cap sweep is published under `--hold-emulate` and labelled as an emulation of an unbuilt mechanic. |
| **THE TWO TERMINAL DEEP-ENTRY MATERIALS HAVE NO NAMED SINK, AND THE FIRST ONE IS 24x OVERSUPPLIED** | A.74 | **OPEN — measured, not fixed** | `graveclaydeep` and `deepgrave` appear in exactly five files (`materials.ts`, `traits.ts`, `compaction.ts`, `roll.ts` — a station NAME, not a consumer — and one test): no `TOOL_RECIPES` input, no `CAST_RECIPES`, no alloy, no `materialCosts`. `umberjade` has exactly one named consumer, `deepcutter` (`forge.ts:268`), which wants **6**; a 2h active arm ends holding **146** (`sim-out/fork-progression.md` s0) | Both remain usable through the GENERIC paths (cast into parts, drill alloys, tool heads), so they are not inert — but nothing in the game ASKS for them. **This is why the packed side of every shop fork fails a progression score:** compaction produces a resource that has no consumer to be short of, so improving its rate or its rarity (BITE, SETTLE) buys nothing that moves the player down. Any future attempt at the forks should start here, not at the fork. |

| **THE CIRCUIT'S CUT READS — three of §7.3's four example lines have no source** | A.85 | **CUT, not stubbed** — the vocabulary is `avail`-filtered and `circuit.test.ts` asserts these ids are absent | `READS` in `systems/circuit.ts` (8 entries: seam · station · hazard · depth · compaction · charge · heat · surge). §7.3's own examples: `grain = across` (grain cut at bd9f3ae, `follow` went with it — drills.ts:129), `abrasive < 4` (no Sieve; Siever's Rest 98 is a station with no machine), `pressure > 88%` (`systems/pressure.ts` is Cinder's and reads a constant in Loam), `output = flawless` (`refineryUnlocked` is Ferrite mastery 3, so a Loam circuit has no refining machine). Actions: `route to the Hold` (no pipes — the Crusher is hand-fired off the Hold; `bank the Surge` is the honest Loam form), `hold the Line` (t4, Verdance), `run the Press` (Pressyard 120) | A read that cannot change an action is a readout, and a name against no mechanism is the deceptive stub PILLARS warns about. Each of the eight that shipped is paired in test with two Loam-producible world states that select different rows — that is the bar, and it is why the list is eight and not twelve. **What the Circuit would want next, in order:** a machine's own condition (needs E2, §7.2 — one condition value per machine); a FILTER as an action (needs Sorting, automation t3 — the Circuit can only route a whole machine, and filters are the real fix for §25.5's first problem); a trait as a read (needs E1, §7.4 — until traits transmit, `WHEN the stock is keen` says the same thing as `WHEN the seam is X`); a Proof as a gate (§25.2 earns tier 6 with "satisfy one Proof using a standing condition", and no proposition currently reads a standing rule, which is why Loam's gate is routing-by-hand); crews (t7 — §7.3 says the Circuit "retroactively makes crews situational", and the drift that walks them is not built). |
| **The Circuit can only route a WHOLE machine, so §25.5's first problem is only half-answered** | A.85 | OPEN — mitigated, named, not solved | `ACTS` `run` in `systems/circuit.ts` skips any stack in `qol.pins` (the RESERVE flag §25.5 asks to ship with, which this build already had). But a bay action sets EVERY drill, and a Crusher action takes the biggest unpinned stack | The mitigation is real and tested (`circuit.test.ts` §7), and it is a blunt instrument: "reserve this one stone" is expressible, "crush only stone under Fair" is not. That is Sorting's job (automation t3), so this stays open rather than being patched with a second ad-hoc filter on the Crusher act. |

| **THE COLLAPSE FAST-FORWARD IS GATED AT DEPTH 120, SO IT FIRES FOR THE LAST THIRD OF A LOAM ARC** | A.86 | **OPEN — measured, deliberately NOT moved (needs a ruling)** | `shoringdeep` at depth **120** in `content/shell1/roll.ts:178`, `wreck: 'SHORING RIG'` — the keystone table (spine:568) and the Roll (spine:340) both author it there. 5h active, seed 11, `scripts/sim.ts --shore` one flag apart: rig raised at **205m of 300m**, four bands timbered by 247m, chain reaching **33m of a 141m** climb, **3 of 8** Collapses fell through it | **Baseline: 88.4% of descend steps and 89.3% of descend dust re-cover ground already walked** (1112/1258), 157.3 steps per Collapse. **With shoring as it actually lands: 141.4 steps per Collapse (−10.1%)**, the drift removing 94 steps over 3 falls. The mechanic works; its GATE is two thirds of the way through the shell it serves. Same family as the A.42 drill-bay inversion and not the same bug — shoring crosses no wall, it removes a TAX, so nothing is softlocked and the standing rule is not violated. But §21.1's own claim is *"by Collapse 8 the first 60 depths take under a minute"*, and at this gate the player reaches Collapse 8 roughly when the rig arrives. Levers, all rulings: move the wreck's payload shallower, split the rig into a cheap shallow tier and a deep one, or accept that the fast-forward is a late-shell and cross-shell system (which it cannot be until other shells author Rolls — see the row below). **Do not tune the price to compensate**; the price is derived (3 walks of the band, `SHORE_PAYBACK`) and re-deriving it would hide a gate problem in a number. |
| **A DRIFT DIES AT THE BREACH, AND SHORING EXISTS IN ONE SHELL** | A.86 | OPEN — both already authored elsewhere in the spine, neither built | `shellRoll` returns `[]` for all six shells but Loam (`systems/roll.ts:66`). Breach survival is spine:1539 — challenge 2, **THE LONG FALL** ("you may not Collapse" → *drifts survive Breach*). Recursion survival is spine:3424 — **THE LONG STAIR**, a Seat: "Brick + cast parts, continuously. Every drift you own becomes permanent through Recursion" | LAW 7's four phases for this system are authored and this pass ships the first: survives Collapse → survives Breach (a challenge reward) → survives Recursion (a Seat) → walked by crews (§25.4, "assign it to a drift"). The second is blocked by the standing ruling that challenge wiring is its own phase; the third by the Seats not existing; the fourth by crews. The FIRST-order blocker is none of those — it is that six shells have no authored Roll, so there are no bands to timber anywhere but Loam. That is the same wall A.84 (remains) and A.85 (the Circuit's world reads) both hit, now three phases running: **six Rolls is the single highest-leverage content pass left in the project.** |
| **The shoring policy is the first thing in this harness that has ever poured a cast part** | A.86 | OPEN — harness gap, named, half-closed | `scripts/sim.ts` had no `castPart`/`chargeCrucible` dispatch anywhere before this phase; `casting.rack` was empty in every run in the project's history | The first shoring arm came out **bit-identical to its baseline** with the rig standing and not one band timbered, because §9.4 prices a band in Brick AND cast parts and the policy could not pay half the price. Two harness bugs under it, both of the ledgered "a sim result is a claim until the harness is verified" family: (1) no reserve, so `buyIfUnder` drained the Brick bank below the rig's 400 every tick and it was never once affordable; (2) charge-and-pour in the same tick, which always fails because a melt takes time. Both fixed for the shoring path only. **The rest of the harness still never pours**, so the Crusher (2 cast parts) and every machine tier have never been exercised by a sim either — that is a wider gap than this phase closed. |

Anything cut in a future phase gets a row here with the same columns, a `Verified against`
reference, and an honest reason, so nothing deferred is silently dropped.

